[package]
name = "kernel-api"
version.workspace = true
edition.workspace = true
rust-version.workspace = true

[dependencies]
# 最小依赖验证：serde + tokio 是后续内核的基础依赖
serde = { version = "1", features = ["derive"] }
serde_json = "1"
