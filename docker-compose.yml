# =============================================================================
# Agent OS — Docker Compose（Redis + Frontend）
# Agent 在宿主机运行：python -m channels.websocket.app_factory
#
# 构建上下文说明：
#   - frontend: context 为仓库根目录（.），以便 Dockerfile 读取 packages/ 离线包
#   - redis: 直接使用 Docker Hub 镜像；首次拉取由 start_web.bat 带镜像前缀回退
# =============================================================================

services:
  # ---------------------------------------------------------------------------
  # 前端 (Python 静态文件服务器)
  # ---------------------------------------------------------------------------
  frontend:
    build:
      # 上下文设为仓库根目录，以便 COPY 根目录下的 packages/ 离线包
      # Dockerfile 内的 COPY 路径已相应加上 frontend/ 前缀
      context: .
      dockerfile: frontend/Dockerfile
    image: agent-os-frontend:latest
    container_name: agent-os-frontend-036fa
    restart: unless-stopped
    ports:
      - "5189:5188"
    environment:
      # Agent 跑在宿主机，前端通过 host.docker.internal 连接
      - BACKEND_URL=http://host.docker.internal:8888
      - BACKEND_WS_URL=ws://host.docker.internal:8888
    depends_on:
      redis:
        condition: service_healthy
    networks:
      - agent-net-036fa
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:5188/health').read(); print('ok')"]
      interval: 30s
      timeout: 5s
      start_period: 10s
      retries: 3

  # ---------------------------------------------------------------------------
  # Redis
  # ---------------------------------------------------------------------------
  redis:
    image: redis:7-alpine
    container_name: agent-os-redis-036fa
    restart: unless-stopped
    ports:
      - "6380:6379"
    command: >
      redis-server
      --maxmemory 256mb
      --maxmemory-policy allkeys-lru
      --appendonly yes
    volumes:
      - redis-data-036fa:/data
    networks:
      - agent-net-036fa
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      start_period: 10s
      retries: 3

volumes:
  redis-data-036fa:

networks:
  agent-net-036fa:
    driver: bridge
