# 插件 / 内核逐个审查 · 进度追踪

> 任务：逐个过审项目里所有插件与内核 crate 的「功能正确性」与「测试正确性」。
> 规则依据（执行中严格遵守）：
> - `config/rules/testing_rules.md`（测试金字塔 / 命名 / Mock / 覆盖 / 禁止行为 / 意图测试 / §9 追溯标记）
> - `reports/audit_round3/_RECHECK.md`（复测结论：300+ 迁移遗留失败、覆盖率门禁缺位、孤儿脚本）
> - `docs/test_traceability.md`（追溯矩阵 @feature/@vision/@audit/@ci）
> - `config/rules/` 其余规则文件（按需）

## 心跳锁（防并发会话冲突）
- `session_active`: main-coordinator (background-agent-loop 模式)
- `last_heartbeat`: 2026-08-12 启动
- 机制：主会话作为协调者，分波次派发后台审查 Agent；Agent 只读实现、可对测试文件追加 §9 追溯标记，其余改动回报主会话裁决。
- **状态：42/42 审查全部完成（2026-08-12）。触发器（后台 Agent 续跑循环）已完成使命并停止派发。**

## 状态图例
- ⬜ pending · 🔄 in_progress · ✅ done · ⚠️ done-with-issues（已记录/暂缓）

## 审查维度（每个模块逐项核对）
1. **功能正确性**：`plugin.json` 声明 vs `server.py` 实现 是否一致；死代码/迁移遗留/坏 import/明显 bug。
2. **测试正确性**（testing_rules）：命名规范、测试隔离、Mock 策略、断言充分、意图（WHY）。
3. **追溯标记**（§9 / test_traceability）：每个测试文件头是否有合法 `@feature`(+`@vision`/`@audit`/`@ci`)。
4. **回归对照**（_RECHECK）：是否命中迁移遗留失败、覆盖率门禁、孤儿脚本等问题。

## A. Python 系统插件（plugins/shared/system/）
| # | 模块 | 状态 | 功能 | 测试 | 标记 | 备注 |
|---|------|------|------|------|------|------|
| 1 | tasks | ✅ | 健康 | 44/44 | 已有(FP-MIGR) | P1迁移债:_task_state/timer_manager引用已删模块(静默降级);P2死代码Shim |
| 2 | llm | ✅ | 健康 | 14/14 | 已补1 | P2死代码Shim;test_prompt_audit标记待裁定 |
| 3 | memory | ✅ | 健康 | 6/6 | 已补1 | P2 manifest缺compress;search无user_id;CI未接入 |
| 4 | hindsight_memory | ✅ | 健康 | 19/19 | 已补2 | CI未接入路径 |
| 5 | isolation | ⚠️ | **P1安全** | 70/71 | 已补11 | P1:checkpoint路径穿越/docker注入/sandbox可逃逸;详见发现 |
| 6 | channel_api | ✅ | 核心健康 | 67+12 | 已补4 | P1潜伏:pipeline.registry引用;rate_limiter旧前缀 |
| 7 | channel_cli | ⚠️ | P1迁移 | 0专用 | — | P1:cli_main.py:164,262 _PROJECT_ROOT parent深度错;REPL零单测 |
| 8 | channel_gateway | ✅ | 健康 | 37/37 | 已补6 | P2:server.py:145读私有;audit#10实为测试重复 |
| 9 | channel_dingtalk | ✅ | 健康 | 39+ | 已补2 | P2:_handle_event空type兜底;token不校验HTTP |
| 10 | channel_feishu | ⚠️ | **P1** | 40+ | 已补3 | P1:stream_client.py:306 headers→header键错致全事件误分发 |
| 11 | channel_qq | ⚠️ | **P1** | 死测试 | 已补1 | P1:onebot反向WS无token鉴权;test_qq_channel.py死代码 |
| 12 | channel_wecom | ⚠️ | **P1** | 仅导入 | — | P1:crypto.py:100签名非常量时间;加解密零单测 |
| 13 | approval | ⚠️ | P2 | 无专用 | — | P2:wait_for_choice无try/finally→异常泄漏+管道挂死 |
| 14 | review | ⚠️ | **P1** | 4/4 | 已补1 | P1:store_report生产无接线→持久化死路径,卡running |
| 15 | rollback | ⚠️ | **P1×3** | 2红/33 | 已补1 | P1:manager.py:334墙钟比较致回滚静默no-op;DB层死代码;GitReverser非幂等 |
| 16 | artifacts | ✅ | 健康 | 30/30 | 已补1 | P2:annotation list total语义;纯内存非FP-DB |
| 17 | connectors | ⚠️ | P2 | 98/6 | 已补6 | P2:registry读方法无锁;6失败=channels.*死测试 |
| 18 | cost_control | ⚠️ | **P1×2** | 无单测 | — | P1:check→record TOCTOU竞态+record不强制上限;P2:gpt-4o编码错 |
| 19 | monitoring | ⚠️ | **P1** | 死(src) | 已补1 | P1:active_requests只增不减→误告警;test是src.*死文件 |
| 20 | multimodal | ⚠️ | **P1×2** | 33规范 | 已补3 | P1:storage路径穿越(PoC);asr.py:112 parents[2]应[4] |
| 21 | scene | ✅ | 健康 | 50+ | 已补1 | P2:server.py:17-19死sys.path;test_scene未进CI |
| 22 | workspace | ✅ | 健康 | PASS | 已补2 | P2:import风格不一;routes_workspaces /api/v1死前缀 |
| 23 | human_interaction | ⚠️ | **P1** | 0专用 | — | 被审目录是__pycache__死尸(impl在tools/human);P1:超时任务悬挂+非幂等;零单测 |
| 24 | evaluation | ✅ | 健康 | 4/4 | 已补1 | P1:http.handle端点无测;_results无界增长 |
| 25 | feature_matrix | ⚠️ | **P1** | 0测试 | — | P1:web/test-asset.txt宣传未实现端点;零测试 |
| 26 | demo_widget | ✅ | 安全 | 17/17 | 已补1 | P2:缺on_unload;不泄漏内部信息 |

## B. Python 管道 / 工具 / SDK（plugins/shared/{pipeline,tools} + plugins/sdk + plugins/builtin_tools）
| # | 模块 | 状态 | 功能 | 测试 | 标记 | 备注 |
|---|------|------|------|------|------|------|
| 27 | pipeline (input/output stages) | ✅ | 高质量 | 75/80 | 已补9 | P1:knowledge_inject矛盾+死代码;security_check指纹不全;5失败=Win tzdata |
| 28 | tools (bash/search/memory/task/...) | ⚠️ | **P1×3** | 88(余不可加载) | 已补5 | P1:download SSRF旁路+save_path穿越;web_ext无SSRF;media NameError被noqa消音 |
| 29 | sdk | ✅ | 高质量 | 195/195 | 已补6 | P2:test_logging_system写cwd;exceptions/孤儿包 |
| 30 | builtin_tools | ⚠️ | **P1** | 48(需conftest) | 已补1 | P1:CI装配缺陷致48测试collection error(0 CI贡献);plugins/builtin_tools空壳 |

## C. Rust 内核 crate（kernel/crates/）
| # | crate | 状态 | 功能 | 测试 | 标记 | 备注 |
|---|------|------|------|------|------|------|
| 31 | core | ✅ | 无P0 | 77/77 | 已补7 | P2死代码TargetType/CompositePluginConfig/PluginContext死字段;覆盖45% |
| 32 | engine | ✅ | 无P0 | 110/110 | 已补2 | P2 DEBT config_hash占位/RouteNext简化;6 src内嵌tests待补标记 |
| 33 | plugin-loader | ⚠️ | P1 | 83/83 | 已补1 | P1:registry register_route_signals重注册泄漏全局signal;P2 find_http_route模板歧义 |
| 34 | invoker | ✅ | 无P0 | 34+1ign | 0(无tests/) | P1:check_permissions文档承诺未实现;P2 unload_if_idle用load()副作用;T5#16准确 |
| 35 | mcp | ✅ | 无P0 | PASS | 已补4 | P2:InitFailed/ProcessCrashed死代码;initialized标志未校验;进程树/stderr/EOF扎实 |
| 36 | api | ⚠️ | **P1安全** | PASS | 已补14 | P1:配置写入面零鉴权(PUT config/actions_execute/compat_routes);SQL注入已参数化(安全);CORS/穿越正确 |
| 37 | config | ⚠️ | P1 | PASS | 已补2 | P1:loader.rs:170 {{path:}}无穿越防护;P2 config_center热重载未接线;load_env_file不剥引号 |
| 38 | session | ⚠️ | P1 | PASS | 已补7 | P1:coordinator.rs:225-227 kick_old_total双计数(被>=1掩盖);P2 event_bus桶无GC |
| 39 | tenant | ✅ | 优秀 | 内嵌6 | — | task_local隔离正确,100%覆盖,无bug |
| 40 | hooks | ✅ | 健康 | 内嵌6 | — | 无tests/目录但6内嵌测试(T5#16准确);无bug |
| 41 | native-sdk | ✅ | 健康 | 内嵌3 | — | box_from_raw安全;无bug |
| 42 | integration-tests | ✅ | 健康 | 42P/7ign | 已补5 | **T5#9已修**(bench_baseline现带断言);config_injection Win /tmp已ignore;_RECHECK表C#9过时 |

## 详细发现（逐模块填写）

### Wave 1（8 模块，2026-08-12）

**1. tasks** ✅ 44/44。P1迁移债：`_task_state.py:205,575`+`timer_manager.py:193` 引用已删 `pipeline.registry`/`config_center`（try/except 静默降级，已标 DEBT）。P2 死代码 `server.py:28-44 _ConfigCenterShim`。标记：test_tasks_plugin.py 已有(FP-MIGR)。

**2. llm** ✅ 14/14。P2 死代码 `server.py:32-42 _ConfigCenterShim`。已补 `test_config_injection.py`(FP-0.2.CFG)。`test_prompt_audit.py` 标记归属存疑待裁定。

**3. memory** ✅ 6/6。P2：`plugin.json:10-23` 缺 `memory.compress` 声明(manifest漂移)；`server.py:177-194` search 无 user_id 作用域。已补 test_compress.py。**CI未接入**：python-plugins-test 未含 memory 路径。

**4. hindsight_memory** ✅ 19/19。无bug。已补 2 文件标记。**CI未接入**。

**5. isolation** ⚠️ 70/71（1环境耦合失败：test_isolation_workspace_mount 在 WSL docker 下）。**P1安全**：
- (a) `checkpoint.py:83,166,179,185,202` task_id/original_path 无边界校验 → 路径穿越，可经 LLM 输入任意写宿主文件；
- (b) `providers/docker_provider.py:794,841` _file_op/write path 未转义 → shell 注入(限容器内)；
- (c) `sandbox.py:56,281-302,327-335` safe_builtins 含 getattr/globals/type 可逃逸 + max_memory 从未 setrlimit。
P2：死代码 cua/e2b_provider.py；`self_heal.py:43-48` 不可达分支谎报成功；`permission_checker.py:79,137` CUSTOM 范围无 os.sep 边界。已补 11 测试标记(timing 文件带 T5#3)。

**6. channel_api** ✅ 核心 67+12 通过。P0 无；路由迁移完整，鉴权健全(bcrypt+JWT验签+admin默认拒绝)。P1潜伏：`rate_limiter.py:71` 旧 `/api/v1/auth/` 前缀(未接入)；`routes_threads.py:667,778,824`+`routes_external_chat.py:105` 引用已删 `pipeline.registry`(休眠域,迁移即崩)。P2 `server.py:81` 过时路由列表。`test_p1_api_unified.py` 坏(测已删0.1包)。已补 4 测试标记。

**31. core** ✅ 77/77。P2 死代码：`types.rs:392 TargetType`、`types.rs:921 CompositePluginConfig`、`types.rs:185,187,208 PluginContext session_id/task_id 死字段`。覆盖 45% 因 trait 行为未测。已补 7 测试标记(FP-0.2.一/T5#16；scaffold FP-T03)。

**32. engine** ✅ 110/110。P2 DEBT：`pipeline_loop.rs:520 config_hash=""` 占位；`pipeline_loop.rs:674 RouteNext::Step` 未真跳转；`store.rs:1502,1519` 死代码。已补 2 测试标记(FP-0.2.〇)。6 个 src 内嵌 tests 待补标记(需 src 写权限)。

<!-- 后续 wave 在此追加 -->

### Wave 4（10 Rust crate，C #33-42）

**33. plugin-loader** ⚠️ P1。双根扫描/manifest 校验/`secure_eq` 常数时间/SHA256 准入健壮。**P1** `registry.rs:137-144` `register_route_signals` 重注册：全局 `route_signals` HashSet 只 insert 不先移除旧 signals → `clear_plugin` 后旧 signal 永久泄漏，`has_route_signal` 永真（与 tools/resources/http_routes 三套原子替换不一致）。P2 `find_http_route` 多模板歧义迭代序非确定。83/83 全绿。已补 1 标记。

**34. invoker** ✅ 无 P0。host 三路分发/single-flight spawn 锁/Pull 热加载 TTL/空闲 GC/PYTHONPATH 三段注入设计严谨。P1 `invoker.rs:1019-1059` `check_permissions` 文档承诺的"声明含糊检测"未实现（文档/代码二选一对齐）。P2 `unload_if_idle` 用 `load()` 取 host_type 有实例化副作用（应用 `get_manifest`）。无 tests/ 目录（T5#16「🟡 结构未补/运行已覆盖」准确，src 内嵌 36 测试）。34 passed + 1 ignored + 1 Windows cdylib 崩溃（环境假象）。

**35. mcp** ✅ 无 P0。进程树清理（Unix 进程组 SIGKILL / Windows taskkill /T/F）、stderr lossy 解码、stdout EOF `pending.clear()` 快速失败均扎实。P2 `InitFailed`/`ProcessCrashed` 死代码、`initialized` 标志未校验。4 标记已补。

**36. api** ⚠️⚠️ **P1 安全**：**配置写入面零鉴权**——`routes.rs:587,869,1146`（agent/plugin/pipeline config PUT）、`routes.rs:641`（actions_execute）、`server.rs:1151`（interaction_response）、全部 `compat_routes`（threads CRUD/plugins reload）无任何 `require_*` 鉴权，任何能访问内核端口的客户端可改写配置/触发命令。**SQL 注入安全**（表/列动态白名单 + prepared statement + tenant_id 自动追加）、CORS（精确匹配防前缀绕过）、静态资源（拒 `..`+canonicalize 子树）均正确。db_routes 是鉴权样板但其余端点未跟进。14 标记已补。

**37. config** ⚠️ P1。**P1** `loader.rs:170` `{{path:...}}` `project_root.join(raw_path)` 无 canonicalize/边界校验 → `{{path:../../../etc/passwd}}` 读任意文件拼入配置。P2 config_center 热重载未接线（已知架构债，文档诚实标注）、`load_env_file` 不剥引号不识别 export、`trim_audit` O(n²)。覆盖优先级/env 缺失报错正确。2 标记已补。

**38. session** ⚠️ P1。**P1** `coordinator.rs:225-227` `kick_old_total` 双计数：`register()` 踢旧已 `inc_kick_old()`，`handle_reconnect` 又 inc 一次 → 每次重连踢旧 +2，被 `session_coordinator_test.rs:176` `>= 1` 断言掩盖。P2 event_bus 令牌桶 HashMap 无 GC、`emit_event` 绕过限流（文档已说明）。跨租户隔离（复合主键 `(tenant_id,pipeline_id)`）、连接注册 stale finally 防误删、replay 缓冲语义均正确。7 标记已补。

**39. tenant** ✅ 优秀。基于 `tokio::task_local!`，跨 spawn 不传播是 Tokio 硬语义 → 天然防跨租户泄露。100% 覆盖，6 内嵌测试含 `context_does_not_propagate_across_spawn`。无 bug、无死代码（仅 95 行）。

**40. hooks** ✅ 健康。broadcast 事件总线，best-effort emit 正确。无 tests/ 目录但有 6 内嵌测试覆盖 fan-out/Lagged 恢复/best-effort（T5#16 准确）。无 bug。

**41. native-sdk** ✅ 健康。跨 cdylib 契约，`box_from_raw` 判空 + 双重 Box + SAFETY 注释完整。3 内嵌测试。无 bug。

**42. integration-tests** ✅ 健康。**T5#9 已实际修复**：`bench_baseline.rs` 5 个 bench 现全带耗时断言（`< 5ms`/`< 50ms` 等）并实跑通过——`_RECHECK.md`/test_traceability 表 C #9「❌ 未修」**已过时，应更新为 ✅**。config_injection Windows /tmp 7 个已 `#[cfg_attr(windows,ignore)]` + `temp_dir()`，本地 3 passed/7 ignored 与 CI(Ubuntu 全跑) 一致。42 passed/7 ignored/0 failed。5 标记已补（bench_baseline 带 @audit:T5#9）。

### Wave 3（10 模块，A #21-26 + B #27-30）

**21. scene** ✅ 50+ PASS。干净。P2 `server.py:17-19` 死 sys.path。`tests/test_scene.py` 未进 CI。已补 1 标记。

**22. workspace** ✅ PASS。单 loop 下并发安全。P2 import 风格不一、`channel_api/routes_workspaces.py:22` `/api/v1` 死前缀（router 未 mount）。已补 2 标记。

**23. human_interaction** ⚠️ **P1**：被审目录 `plugins/shared/system/human_interaction/` **仅 `__pycache__` 死尸**（FP-MIGR 孤儿，建议删）；真实 V2 实现在 `plugins/shared/tools/human/service.py`。P1 `_setup_timeout`(:708) 短超时下后台任务悬挂至 +300s 且 `_handle_timeout`(:745) 非幂等（wait_for_choice 与 timeout_handler 双触发）。`channel_cli/cli_main.py:518,524` 仍 import 旧顶层包 → CLI 人机交互静默失效。**HumanInteractionService 全仓零直接单测**。

**24. evaluation** ✅ 4/4。P1 `server.py:286-344` http.handle 三个 metrics 端点无任何测试；`_results`(:123,241) 无界增长。已补 1 标记。

**25. feature_matrix** ⚠️ **P1**：`web/test-asset.txt` 宣传的 `/assets/` 静态端点既未在 plugin.json 声明也未在 server.py 实现（误导死文件）；**零测试**。

**26. demo_widget** ✅ 17/17。安全（不泄漏内部信息、无 XSS 面、record_metric 签名对齐 SDK）。P2 缺 on_unload。已补 1 标记(FP-0.2.三)。

**27. pipeline** ✅ 75/80。**安全闸门稳健**：security_check 硬底线不可被隔离绕过、prompt_build 占位符递归双保险、context_window_guard fail-closed。P1 `knowledge_inject/plugin.py:64` error_policy ABORT 与 docstring FALLBACK 矛盾 + `_format_*`/`_filter_by_relevance` 死代码（后者引用错误 tags 位置）；`security_check/plugin.py:555-602` `_make_signature` 指纹不含 env 等参数 → approved_remember 可能对未哈希差异自动放行。5 失败全为 Windows tzdata（插件回退逻辑正确）。已补 9 标记(FP-0.2.〇)。

**28. tools** ⚠️⚠️ **P1×3 + 系统性迁移债**：
- **P1 安全** `download/tool.py:216-220,266-272` `skip_ssrf_check` 作为用户可控入参暴露 → LLM/提示注入可旁路 SSRF；`save_path`(:278-283) 无 workspace 拘束 → 目录穿越写。
- **P1 安全** `web_ext/tool.py:214-244` `_check_url_security` 无内网 IP DNS 校验 → 可触达元数据/localhost/内网。
- **P1** `media/{image,tts,video,music}_generate.py` 引用 `ProviderChain` 但未 import，用 `# noqa: F821` **消音** linter → 用户传 provider 时 NameError。
- **FP-MIGR P0**：约 10 个工具（download/web_ext/media/task_submit/task_evaluate/human/resource_merge/hot_swap/triggers_ext）顶层或惰性 import 已删的 `tools.*/core.*/channels.*/infrastructure.*/pipeline.*` → 不可加载或运行时死路径（`src/` 已删，server.py `if isdir(_SRC)` 守卫使其静默失效）。
- 高质量孤岛：bash（安全模型扎实）、memory（0.2 重写范本 + IDOR 防护）、search、simple、task 主体。已补 5 标记。

**29. sdk** ✅ 195/195。SDK 作为 0.2 第三方插件协议地基扎实（McpServer JSON-RPC/反向调用/Win32-Unix 双路径 stdin）。P2 `test_logging_system.py:912-926` 用相对路径写真实文件到 cwd（测试卫生）；`exceptions/` 子包未接入 `__init__`（孤儿）；`tool_types.py:444`/`builtin_tool.py:55` `to_runnable` 懒导入已删 `core.runnable`。已补 6 标记(FP-0.2.一)。

**30. builtin_tools** ⚠️ **P1（CI 装配 bug）**：`plugins/shared/tools/builtin_tools/tests/` 缺 sys.path 装配（无 conftest + ci.yml PYTHONPATH/install 均未含其 src）→ CI `python-plugins-test` 收集这 48 例即 collection error → **48 个有效测试 0 CI 贡献**（未被 _RECHECK 捕获）。补 src 到 PYTHONPATH 后 48 passed。修法：加 `tests/conftest.py` 注入 src。`plugins/builtin_tools/` 是 lingxi 残留空壳（建议删）。已补 1 标记。

### Wave 2（14 模块，A 组 #7-#20）

**7. channel_cli** ⚠️ **P1**：`cli_main.py:164,262` `_PROJECT_ROOT` 在迁移后 parent 深度少一级（指向 `plugins/` 非仓库根），standalone CLI 配置加载失效（不影响 sidecar）。REPL 零单测。

**8. channel_gateway** ✅ 37/37。实现干净。P2 `server.py:145` 读私有 `_adapters`。audit #10 实为 `test_channel_gateway.py` 与 `test_gateway.py` 重复（非脆性 impl-detail），建议合并。已补 6 标记(FP-0.2.七；test_channel_gateway +T5#10)。

**9. channel_dingtalk** ✅ 39+。签名 HMAC-SHA256 正确。P2 `_handle_event` 空 type 兜底、token 不校验 HTTP。已补 2 标记。

**10. channel_feishu** ⚠️ **P1**：`stream_client.py:306` `data.get("headers",{})`（复数）键名错，飞书 v2 实为 `header`（单数），致 `event_type` 恒空 → `:317` 兜底分支对所有事件恒成立 → 非消息事件也被当消息喂 LLM。`:312` ACK 用对的单数，证复制粘贴残留。无 `_handle_event` 单测拦截。已补 3 标记。

**11. channel_qq** ⚠️ **P1**：`onebot_client.py:101-104` 反向 WS 监听 `0.0.0.0:8080` 无 access_token 鉴权 → 任何连入者可注入伪造消息。`test_qq_channel.py` 整文件是 0.1 死测试（`pipeline.`/`channels.` 不可 collect）→ QQ 渠道 0.2 下零有效测试。已补 1 标记。

**12. channel_wecom** ⚠️ **P1**：`crypto.py:100` `calculated != signature` 非常量时间比较 → 应 `hmac.compare_digest`。加解密算法本身正确（AES-256-CBC+PKCS7，解密校验 corp_id，先验签后解密）。**加解密/验签零单测**——最敏感渠道测试最薄。

**13. approval** ⚠️ P2：`server.py:133-151` `wait_for_choice` 外无 try/finally → 异常时 `_suspended` 泄漏 + 管道挂死。审批闸门超时即恢复语义存疑（=绕过）。无 approval 专用测试。

**14. review** ⚠️ **P1**：`server.py` `store_report` 在生产中**无任何调用方**（无工具/无事件订阅/无 on_load）→ 报告落 Hindsight 的持久化路径是死路径，review 状态永久卡 `running`，`get_report` 永远轮询不到 completed。test_review_persistence 4/4 通过但测的是死路径。已补 1 标记。

**15. rollback** ⚠️⚠️ **P1×3**：
- **P1 真 bug（2 测试红）** `manager.py:334` 用墙钟 `op.created_at > checkpoint.created_at` 筛选，Windows ~15ms 精度同 tick → 全过滤 → 回滚静默 no-op（用户以为回滚了实际没做）。应改按 sequence（`_sequence_counters` 已单调维护，只是没存进 checkpoint）。2 红：`test_按checkpoint回滚只撤销其后操作`、`test_to_checkpoint优先级高于steps`。
- **P1** 整个 DB 持久化层是死代码（`_db_models.py` 仓库不存在）→ `RollbackManager(session=非None)` 运行时 ModuleNotFoundError。
- **P1** `reversers.py:245` GitReverser `git reset --soft HEAD~1` 非幂等且误回滚（有后续 commit 时回退错误 commit；应用 `before_state.commit_hash`）。
已补 1 标记。

**16. artifacts** ✅ 30/30。纯内存 sidecar（**证伪 FP-DB**，归 FP-0.2.二）。P2 `annotation_service.py:122` `total` 是过滤后计数非总数（分页语义不一致）。已补 1 标记。

**17. connectors** ⚠️ P2。策略/健康检查/降级设计完整。P2 `registry.py:73-155` 读方法不持锁 → 并发 `RuntimeError: dict changed size`。6 失败 = `channels.*` 死测试（非插件 bug）。已补 6 标记。

**18. cost_control** ⚠️⚠️ **P1×2**：
- **P1** `budget_manager.py:125-251` check→record TOCTOU：`check_budget` 与 `record_usage` 是两次独立加锁，无 reserve/hold → 并发 LLM 调用可全部过 check 再各自 record → per-task/per-session 预算被真实突破（全局默认近无限不致 bite）。
- **P1** `record_usage` 从不强制上限，仅告警不 raise → 超额仅 INFO/WARNING 无拦截。
- P2 告警回调持锁 await、同步读无锁、`tokenizer.py:60-73` gpt-4o 因前缀序被错映射 cl100k_base、reset 端点裸改私有无锁。
**无任何 cost_control 单测**。已补（test_track_cost_update_event 已有 FP-MIGR）。

**19. monitoring** ⚠️ **P1**：`performance_monitor.py:346-353` `record_llm_request` 既 `request_count++` 又 `active_requests++`，而唯一能减的 `record_llm_response` 未在 plugin.json 暴露为工具 → 经公开 API 调用后 `active_requests` 只增不减 → 持续误报"LLM 活跃请求过多"high 告警。`test_monitoring_validation.py` 是 `src.*` 死文件（已被 conftest collect_ignore_glob 软跳过）。已补 1 标记。

**20. multimodal** ⚠️ **P1×2**：
- **P1** `storage.py:131-133` `DiskFileStorage._meta_path` 直接拼 file_id 无清洗 → 路径穿越（已 PoC：`save('../../evil_outside',{})` 落盘 base_dir 外）。canonical `tests/multimodal/test_disk_storage.py`(33 passed) 未覆盖 `..`。
- **P1** `asr.py:112` `parents[2]` 推项目根错（0.1 布局残留），应为 `parents[4]` → `config/models/asr.yaml` 永不加载，走环境变量回退。
test_asr_service/test_multimodal_storage 是迁移死文件。已补 3 标记。

<!-- 后续 wave 在此追加 -->

## 汇总统计
- 完成 / 总数：**42 / 42 ✅ 审查全部完成**；**16 / 16 修复功能点全部完成（Wave F1+F2+G）**；**Wave H/I 全部完成（2026-08-13）**
- 发现问题：P0×0、P1×~30（审查发现）、P2×~60
- 已补 §9 追溯标记：**~125 个测试文件**（含新修复测试，本次 +1：test_hot_swap_resource_merge_migration.py）
- 已修复：**27 项 P1 真 bug/安全项**，全部带测试验证（本次 +2：F-MIGR-2 剩余迁移清理、F-HI-1 重做）
- **触发器（后台 Agent 续跑循环）：已随全部任务完成停止派发（无 cron 需删除）**
- **剩余可接续清单：全部 0-8 项完成（⑤b 于本次会话恢复执行并完成）**

## ⚠️ 重要事件：并发覆盖 + 网络不稳定（2026-08-13）
并行 agent 在调试时执行 git stash/reset 互相覆盖 tracked 修改 + ZCode subtask auto-save 机制 → 大量 tracked 修复被 reset 清空（新建文件 untracked 保住）。已重做 14 项（见下），但剩余 tracked 修复仍需重做。**网络对 subagent 持续不稳定**（多轮批量失败），剩余项需主会话顺序做或网络恢复后批量 agent（prompt 必须严格禁 git）。

### 已重做完成（14 项，全绿）✅
config 穿越防护 / session 双计数 / plugin-loader 泄漏 / monitoring(start+end+collector) / wecom 时序 / asr 配置路径 / multimodal 穿越 / approval 超时拒绝 / checkpoint 穿越(26) / docker 注入(10) / sandbox 白名单(75) / **download 迁移(8)** / feishu(保住) / knowledge-ABORT(保住)

### 剩余可接续清单（tracked 修复被 reset，测试文件都在 auto-save，按测试重做实现即可）
> **状态：0-8 全部完成（2026-08-13，本次会话完成 ⑤b）**。每项已在上方「主会话续做记录」/「Wave H/I」标注验证数字。
> 每项：[测试位置] [改法要点] [文件范围] [当前状态]

0. ✅ **F-API-1 + F-TENANT-B-KERNEL 已完成（主会话 2026-08-13）**：middleware `write_surface_auth`（server.rs，路径白名单+method 分写/读）+ `require_*` pub(crate) + 17 个既有测试补 admin token + `tenant-context.get`（capability_router，无 scope 回退 default）+ 依赖 pin（idna_adapter 1.1.0，rustc 1.85 兼容）+ traits 字段核对（PluginManifest 无 mcp_endpoint，已移除测试里误加字段）。**api 全量 240 passed / 0 failed**。
1. **rollback asyncio 同步化** — `tests/plugins/system/rollback/test_rollback_{db_mode,git_reverser,manager}.py`（65 用例）。
   改法：manager.py `AsyncSession`→`Session`（import + __init__ + get_rollback_manager）；8 个 DB 方法 `async def`→`def` + 去 await；调用点 `await self._xxx_db(...)`→`await asyncio.to_thread(self._xxx_db,...)`（~9 处 + `_get_next_sequence`）；`_list_operations_from_db` 的 `get_checkpoint` 改同步直查；`_db_models.py` 加 `create_db_engine(url)`（`check_same_thread=False` + 内存库 StaticPool）；测试 fixture `db_session` 同步化（create_engine + Session，去 aiosqlite/AsyncSession）。当前 manager.py 是 async 版（27 failed）。
2. **cost_control 预留** — `tests/plugins/system/cost_control/test_budget_concurrency.py`（13 用例）。
   改法：budget_manager.py `__init__` 加 `_task_reserved/_session_reserved`(dict) + `_global_daily_reserved/_global_monthly_reserved`(int)；`check_budget` 锁内检查 `usage+reserved+estimated≤limit` 并预留（task/session/global）；`record_usage` 兑付预留 + 强制上限 raise；decorators.py `@budget_check` 失败释放；`_check_and_reset_periods` 清 global 预留。对外签名不变。当前 budget_manager 是原始版（13 failed）。
3. **api 鉴权面（F-API-1）** — `kernel/crates/api/tests/unauthorized_write_test.rs`（4 用例）。
   改法：db_routes.rs `require_read_role/require_admin_role` 改 `pub(crate)`；routes.rs（put_agent/plugin/pipeline_config + actions_execute）、server.rs（interaction_response）、compat_routes.rs（threads CRUD/plugins reload）加 `require_admin_role`（写面）/`require_read_role`（读面）；5 既有测试（actions_execute/agent_config/plugin_config/pipeline_config/e2e_path_coverage）补 admin token（admin/admin12345 + Authorization）。调用方：前端 axios 拦截器全带 token，安全。
4. **tenant-context.get（F-TENANT-B-KERNEL）** — 新建测试。
   改法：capability_router.rs（:457 占位）实现 `tenant-context.get` 返回 `{"tenant_id": agentos_tenant::current() 或 "default", "session_id":...}`。让 Python 侧 `tenant_data.py` 的 `get_current_tenant_id` 真正生效（不再恒回退 default）。
5. **web_ext 迁移重做** — `plugins/shared/tools/tests/test_web_ext_migration.py`。
   改法：web_ext/tool.py（tracked 丢失，和 download 同款）顶层 0.1 import → agentos_plugin_sdk + url_security；`_check_url_security` 加 DNS 内网 IP 比对。参考 download（已迁）+ url_security.py。
6. ✅ **media/task 迁移核验修复** — `test_media_migration.py`(27)/`test_task_submit_migration.py`(31)/`test_task_evaluate_migration.py`(19)/`test_human_migration.py`(15)。
   改法：先跑这 4 个测试看哪些红（tracked tool.py 丢失的会 ModuleNotFoundError）；丢失的按测试重做。media 参考 `_media_core.py`（保住）+ hindsight memory_backend capability 调用（`media.generate`，不降级）；task_submit/task_evaluate 参考 `_eval_core.py`（保住）+ agentos_plugin_sdk；human 接 V2 `plugins/shared/tools/human/service.py`。**全部完成（本次会话）**：task_evaluate 19 passed、human 15 passed（task_submit/media 前次完成）。
7. **F-REVIEW-2 review 真实完成** — `plugins/shared/system/review/test_review_persistence.py`。
   改法：选最小可行——review trigger 启动后记 running+run_id，get_report/后台轮询查 run 状态（需 capability `pipeline-executor.get_run_status`，最小加），完成才落报告+completed；或回落 F-RV-1 乐观 + docstring 标注。先调研 engine/capability_router/hooks。
8. **F-TENANT-B-T2 其余插件按租户** — `tests/plugins/shared/test_tenant_data.py`（T1 的 21 用例，扩展）。
   改法：tasks/storage.py、scene/persistence.py、memory(db 路径)、channel_api/routes_artifacts.py(uploads) 按 multimodal 范式（T1 成果 tenant_data.py）加 `tenant_id` 参数 + 默认经 `tenant_data_root`。

### 接续方式
新会话读本文件 + 对应测试文件即可续做。每项改完跑测试验证（命令见各测试路径）。**禁 git 操作**（防覆盖）。网络稳定时可批量派 agent（prompt 严格禁 git + 不同文件分工）。

### 主会话续做记录（2026-08-13，一次一个）
- ✅ **F-API-1 鉴权**：middleware write_surface_auth + require_* pub(crate) + 17 测试补 admin token。api 全量 **240 passed**。
- ✅ **F-TENANT-B-KERNEL**：tenant-context.get（capability_router，无 scope 回退 default）。tenant_context_test 2/2。
- ✅ **rollback 同步化**：manager.py 全同步（AsyncSession→Session + to_thread 调用点）+ sequence 墙钟修复 + ROLLING_BACK 幂等令牌 + _reverse_commit 精确回退 + _reverse_create 目录保护 + models/_db_models 字段补齐。**65 passed**。
- ✅ **cost_control 预留**：F-CC-1+F-COST-2（task/session/global 预留 + record 强制上限 + decorator 失败释放 + reset 清预留）。**13 passed**。
- ✅ **web_ext 迁移**：SDK 导入 + url_security SSRF（DNS 解析 + 内网 IP 比对）。**12 passed**。
- ✅ **⑤a media 迁移**：4 工具 SDK + _media_core + capability 双路径（不静默空转）+ server.py 修正（TTSTool→TtsGenerateTool）。test_media_migration **27 passed**。
- ⏸ **⑤b task 工具迁移（task_evaluate/human）**：~~用户暂缓~~ → **已恢复执行（本次会话，见下）**。
- ✅ **⑥ F-REVIEW-2**：内核加 `pipeline-executor.get_run_status`（查 store.get_run，零 engine 改动）+ review get_report 惰性轮询（completed/failed 落终态，能力缺失降级）。review **13 passed** + api cargo 全绿。残留：生产 run driver 未存在，已 docstring 标注待内核完成事件。
- ✅ **⑦ F-TENANT-B-T2**：tasks/scene/memory/uploads 按租户（tenant_data_root 范式）。tests/plugins/shared **44 passed** + 存量回归无新增失败。
- ✅ **tenant_data parents 修复**：`tenant_data.py:69` `parents[1]`→`parents[2]`（默认数据根从 `plugins/data/` 修正为仓库根 `data/`）。
- ✅ **F-MIGR-2 完成（本次会话，2026-08-13）**：task_evaluate/human 迁移 + hot_swap/resource_merge 清理（详见下方 Wave H/I 更新）。
- ✅ **F-HI-1 重做（本次会话）**：service.py 超时竞态修复在并发 reset 中丢失（漏网项，非 14 项重做清单内），按测试重做——短超时分支（timeout<remind 不提醒直接收敛）+ wait_for_choice 超时取消后台任务（_cancel_timeout_task）+ _handle_timeout 幂等守卫（非 PENDING 直接 return）。**红3→绿7**。
- ✅ **F-TENANT-B 剩余（本次会话）**：config/users 覆盖层咽喉点 `tenant_config_dir`（不自动创建，env AGENTOS_CONFIG_USERS_DIR 可覆盖）+ 迁移 CLI `scripts/migrate_legacy_data_to_default.py`（dry-run/--apply/幂等，实跑验证）。test_tenant_data **21→26 passed**。

### Wave G（2026-08-13，产品拍板后执行）
- **F-SANDBOX-2** 沙箱白名单模式：`sandbox.py` ALLOWED_BUILTINS(32 项) + AST 白名单校验 + safe_builtins 白名单重建（fail-closed，未列出项默认拒绝）。测试 11→75 用例全绿；isolation 回归 194 passed。
- **F-ROLLBACK-DB** DB 持久化补全：新建 `_db_models.py`（RollbackCheckpoint/RollbackOperationLog 含 sequence/error_message）+ manager.py DB 分支实现。**注意：后由主会话改为同步 SQLAlchemy**（原 agent 用 aiosqlite 未声明未安装；主会话补 pyproject 依赖声明 + 引擎工厂 `create_db_engine`（check_same_thread=False，配合 to_thread）+ fixture 同步化）→ 零新依赖、离线可用。19 DB 测试红→绿。
- **F-GITREV-1** GitReverser 业界成熟化：精确回退 `before_state.commit_hash`（删 HEAD~1 兜底）+ 两级校验（rev-parse + merge-base）+ 幂等反转令牌（ROLLING_BACK/ROLLED_BACK 状态）+ 目录保护。11 测试红→绿。
- **F-CLEAN-1** 清理：`human_interaction/`（死尸）、`plugins/builtin_tools/`（空壳）、两处 `lingxi_*.egg-info/`、`feature_matrix/web/test-asset.txt`——引用核查后删除，收集无回归。
- **F-KNOW-1** knowledge_inject 错误策略统一 ABORT：docstring 与代码一致 + `error=e` 真实异常回传。4 测试绿。
- **⚠️ 并发冲突**：执行期间检测到工作区有另一进程（PID 81880）并行修改同一批文件（多次 stash/checkout 覆盖），各 Agent 已二次应用并验证；最终核验全部落盘（54M/2D/19??）。**建议确认无其他会话在并行操作后再继续 Wave H/I。**

### Wave H/I（2026-08-13 已完成 ✅，产品已拍板）
- ✅ **F-TENANT-B** 多租户方案 B（目录隔离）：`data/{tenant_id}/` 数据根 + `config/users/{tenant_id}/` 覆盖层（`tenant_config_dir` 咽喉点，本次补）+ 咽喉点函数（tenant_data.py）+ 现有数据迁移入口（`scripts/migrate_legacy_data_to_default.py`，dry-run/--apply 幂等，本次补）+ 负向隔离测试（T1/T2 含 A/B 隔离断言，T1 本次扩展 21→26）。内核侧 tenant-context 能力已完成（前次）。
- ✅ **F-MEDIA-2** media provider 依赖迁移：经 capability 调用后端服务（tool-executor 优先，service-registry 回落），仿 hindsight memory_backend。四工具未注入时显式 PROVIDER_UNAVAILABLE。test_media_migration 27 passed。
- ✅ **F-MIGR-2** task_submit/task_evaluate/human 迁移；hot_swap/triggers_ext/resource_merge 清理：
  - task_submit：前次完成（31 passed）；
  - task_evaluate：**本次完成**——tool.py 去 0.1 死依赖（core.results/evaluation.types/tasks.types/tools.*/infrastructure），顶层走 SDK + 同目录 `_eval_core.py` + system/tasks 平铺（task_types/service_access）；执行器注入版（构造 `executor=`，未注入 → EVAL_ENGINE_UNAVAILABLE 不静默空转）；litellm 可选依赖防御化；server.py 装配更新（去 src 守卫，注入 tasks 平铺路径）。test_task_evaluate_migration **19 passed**；
  - human：**本次完成**——tool.py 工具壳接 V2 自包含服务（models/service 平铺 + HumanInteractionService 纯内存版 + service 构造注入），WorkspaceAwareMixin 走工具共享层，format_size 就地重建。test_human_migration **15 passed**；
  - triggers_ext：前次完成（trigger_setup 0.2 接线）；
  - hot_swap：**本次清理**——0.1 死依赖（tools.tool_context/channels.cli）→ 0.2 文档化降级工具壳（HotSwapTool + hot_swap_func，参数校验错误码面保留，未注入 → HOT_SWAP_UNAVAILABLE）；server.py 复用 get_tool_definition schema；
  - resource_merge：**本次清理**——tool.py/git_helpers.py 去 0.1 死依赖（tools.builtin.base/tools.types）→ SDK 类型面 + 本目录平铺 git_helpers.py；行为保留。
  - 新增 test_hot_swap_resource_merge_migration.py（§9 标记，20 用例）。tools/tests 全量 **204 passed**。
- ✅ **F-COST-2** 全局事前拦截+中断：并入 cost_control 预留（_global_daily/monthly_reserved + record 强制上限 raise）。13 passed。
- ✅ **F-REVIEW-2** review 真实完成事件：内核 `pipeline-executor.get_run_status` + get_report 惰性轮询落终态。13 passed（残留已 docstring 标注）。
- ✅ **F-APPROVAL-1** 超时拒绝 + 默认超时调大：14 项重做清单内完成（approval 超时拒绝）。
- ✅ **F-MON-1** active_requests 真实化（start/end 配对）：14 项重做清单内完成（monitoring start+end+collector）。
- ✅ **F-HI-1（漏网重做）** human service 超时竞态：短超时按时收敛不误提醒 + wait_for_choice 超时取消后台任务 + _handle_timeout 幂等（非 PENDING 直接 return）+ timeout 任务结束自清理。test_human_service **红3→绿7**。

## 修复功能点追踪（按「功能点 → 测试点 → 修复」TDD 流程推进全部暂缓项）

> 原则：每个修复映射 test_traceability 已有 FP；先落测试点（红）→ 修复（绿）→ 回归验证；测试文件头带 §9 标记。2026-08-13 起自主推进，不再逐一请示。

| 修复功能点 | 映射 FP | 测试点 | 修复方案 | 状态 |
|-----------|--------|--------|---------|------|
| F-CFG-1 config `{{path:}}` 穿越 | FP-0.2.CFG | `path_ref_security_test.rs` 3 用例 | 拒绝对路径 + `..` 段（loader.rs:170） | ✅ 3 测试红→绿；config 全量回归绿 |
| F-HI-1 human_interaction 超时竞态 | FP-0.2.五 | `tests/plugins/tools/human/test_human_service.py` 7 用例 | 短超时分支 + 超时 cancel + 幂等守卫（service.py） | ✅ 红3→绿7；回归 72/331 passed |
| F-RV-1 review 持久化接线 | FP-0.2.六 | test_review_persistence 扩展 3 用例 | trigger_review 成功路径接 store_report（server.py） | ✅ 红1→绿；7 passed |
| F-ISO-1 isolation checkpoint 穿越 | FP-0.2.二 | `test_isolation_checkpoint_security.py` 26 例 | task_id 白名单 + 路径 relative_to 校验（checkpoint.py） | ✅ 红22→绿26；isolation 回归 119 |
| F-ISO-2 docker_provider 注入 | FP-0.2.二 | `test_isolation_docker_provider_injection.py` 10 例 | shlex.quote + 参数化 argv（docker_provider.py） | ✅ 红8→绿10；回归 119 |
| F-API-1 api 配置写入面零鉴权 | FP-0.2.八/FP-DB | `unauthorized_write_test.rs` 4 用例 | 18 端点统一 require_admin_role/require_read_role | ✅ 红3→绿4；api 全量 143+15 文件绿 |
| F-CC-1 cost_control 预算竞态 | FP-0.2.二 | `test_budget_concurrency.py` 9 用例 | check+预留合一 + record 硬上限 + 失败释放 | ✅ 红7→绿9；冒烟 99 passed |
| F-TOOLS-MIG download/web_ext/media | FP-MIGR/FP-0.2.二 | 3 测试文件 35 用例 | 自包含迁移（SDK+共享 url_security）+ 安全随迁 | ✅ 红→绿35；tools 171 passed |
| F-ISO-3 sandbox 定位 | FP-0.2.二 | `test_isolation_sandbox.py` 11 用例 | 文档化受信假设 + blocked_builtins/dunder 收紧 | ✅ 红8→绿13；回归 130 |
| F-CLI-1 channel_cli `_PROJECT_ROOT` | FP-0.2.三 | `test_channel_cli_project_root.py` 2 用例 | parents 4→5 级（cli_main.py:167,266） | ✅ 红2→绿2 |
| F-MIGR-1 迁移死测试清理 | FP-MIGR | 4 重写+1 删除 → 89 用例 | 迁移到 0.2 平铺/删除重复 | ✅ 收集错误 3→0；89 passed |

## 修复批次（P1 真 bug，逐项核实）

### ✅ 已修复并验证（9 项，全部测试绿）
1. **rollback 检查点墙钟 bug（2 红测试）** — `models.py` Checkpoint 增 `sequence` 字段；`manager.py:84` create_checkpoint 记录当前序号；`manager.py:334` 按 `op.sequence > checkpoint.sequence` 筛选（替代墙钟 `created_at >`）。验证：`test_rollback_manager.py` 33pass+2fail → **35 passed**。
2. **feishu 事件误分发** — `stream_client.py:306` `headers`→`header`(飞书 v2 单数)；删死表达式；`:317` 改精确匹配 `== "im.message.receive_v1"`（去 `or event_type==""` 兜底）。验证：feishu 测试 **40 passed**。
3. **wecom 签名时序侧信道** — `crypto.py:100` `!=`→`hmac.compare_digest`（常量时间）。验证：模块 import + compare_digest 生效确认。
4. **multimodal 存储路径穿越** — `storage.py:131` `_meta_path` 拒绝含 `..`/`/`/`\\` 的 file_id。验证：traversal 被 ValueError 拦截 + `test_disk_storage.py` **33 passed**。
5. **multimodal asr 配置路径** — `asr.py:112` `parents[2]`→`parents[4]`。验证：解析到仓库 `config/models/asr.yaml`（exists:True）。
6. **monitoring active_requests 只增不减** — `performance_monitor.py:346` `record_llm_request` 不再自增 active_requests（改由配对 record_llm_response 维护）。验证：增量行已移除确认。
7. **session kick_old 双计数** — `coordinator.rs:224-226` 删冗余 inc_kick_old（register 内已计）；`session_coordinator_test.rs:178` `>=1`→`==1`。验证：cargo test session **全绿**。
8. **plugin-loader route_signal 重注册泄漏** — `registry.rs:137` 改为重建全局集合=所有 plugin 当前 signals 并集（多 plugin 共享 signal 仍正确保留）。验证：cargo test plugin-loader **全绿**。
9. **builtin_tools CI 装配** — 新增 `plugins/shared/tools/builtin_tools/tests/conftest.py` 注入 src 到 sys.path。验证：48 测试 collection error → **48 passed**。

### ⏸ 暂缓（需设计决策 / 迁移前置 / 大手术，已记录 file:line，建议后续处理）
- **api 配置写入面零鉴权**（`routes.rs` PUT config / `actions_execute` / `compat_routes`）— 需统一 admin 鉴权中间件，影响面大，建议专项。
- **cost_control check→record TOCTOU + record 不强制上限**（`budget_manager.py`）— 需引入 reserve/hold 语义，并发模型改动。
- **isolation 安全×3**（`checkpoint.py` 穿越 / `docker_provider.py` shell 注入 / `sandbox.py` 可逃逸+内存未限）— 安全加固，建议逐一评审。
- **tools 安全×3**（`download` SSRF 旁路+save_path 穿越 / `web_ext` 无 SSRF / `media` ProviderChain NameError 被 noqa 消音）— 工具本身是 0.1→0.2 迁移债（不可加载），应随迁移一并修。
- **config `{{path:}}` 穿越**（`loader.rs:170`）、**human_interaction 超时竞态**（`tools/human/service.py`）、**review store_report 无接线**（`review/server.py`）、**channel_cli `_PROJECT_ROOT` 路径**（`cli_main.py:164`）— 各需单独评审。
- 迁移遗留测试清理（test_p1_api_unified / test_qq_channel / test_monitoring_validation 等 src.* 死文件）—— 对应 _RECHECK 阶段 1。
