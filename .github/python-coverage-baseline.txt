# ⚠️ 门禁挂起中（run_gates.py plugins-coverage 内 --skip，2026-09-01 就地裁定）
# ——下方数值不代表当前执法线；插桩度量与 coverage.xml 产出照常，
# 恢复 = run_gates.py 去掉 --skip（详见 scripts/run_gates.py 内注释）。
#
# Python 插件整体行覆盖率基线（line%，只升不降，
# 见 scripts/check_python_coverage_baseline.py；数据源 plugins-coverage gate
# 产出的 coverage.xml line-rate，未取整口径）。
#
# 首次基线（2026-08-20，用户裁决"基线略高于实测留压力"）：
#   - CI 实测 43.77%（run 32333595329 TOTAL 22177/miss 12470；旧
#     fail-under=44 靠显示取整才过——本基线按未取整 line-rate 判定，故
#     44.0 即真实压力线）。
#   - 达到 44.0 后：--init 写入实测，再上调至新实测+压力，commit 留归因。
#   - 只许升不许降；如确有正当理由（度量口径变更），手改并 commit 归因。
#
# 棘轮公式（2026-08-21 用户定稿）：基线 = 实测向上取整到下一个整数百分比
#（例：47.48→48、45.5→46、恰为整数→再 +1），恒高于实测留压力，车道即刻
# 转红是预期设计（红着才有动力往上走）。**已自动化**：checker 绿跑（实测 ≥
# 基线）即自动把本文件写到 floor(实测)+1（只替换数值行），随本批 commit
# 留归因；CI job 内的写入随 job 丢弃，以仓库提交为准。
# 不得定在实测之下保绿——首定 46.50（实测 47.48-1）违背该规则，同日纠正
# 为 48.00（= floor(47.48)+1）。
# 收紧（2026-09-11 覆盖率补测批）：棘轮 87→90——插桩车道全量实测 89.20%
# （30045/33683，7143 测试绿），floor+1=90（棘轮公式）。增量来源 = 本批三件
# 补测：key_pool 49%→98%、router_factory 26.5%→98%、wsl_health 38%→100%
#（提交 e3e949923/b0cad33db）。整体验收闸挂起期内手动收紧，恢复执法后按
# 同公式复核。下一批目标 = llm/adapter、bash/process_manager、
# isolation/_workspace_git_ops、monitoring/server。
# 收紧（2026-09-12 覆盖率补测批二）：棘轮 90→91——插桩车道全量实测 90.34%
# （7459 测试绿，floor+1=91 棘轮公式）。增量来源 = 本批五件补测：
# llm/adapter 63.7%→86.4%（47 用例）、bash/process_manager 72.9%→88.4%（73
# 用例）、monitoring/server 77.2%→77.5%+解析面补齐（17 用例）（提交
# 5a74b4f20/902ecaeef/5c850f877/421035fd2）。下一批目标 =
# task_submit/tool、context_window_guard、isolation/manager、
# channel_cli/cli_output_adapter。
#
# 收紧（2026-09-13 覆盖率补测批三）：棘轮 91→92——插桩车道全量实测 91.20%
# （31314/34336，7522 测试 0 失败，489s），floor+1=92（棘轮公式）。增量来源 =
# 本批三件补测：channel_cli/cli_output_adapter 58.6%→100%（+35 用例至 61，
# 输出面/编码清洗/确认交互全覆盖）、isolation/server 42.5%→100%（+24 用例，
# 工具面参数翻译/生命周期/配置 watcher）、monitoring/server 车道缺行
# 122→5（+43 用例，http 路由分发/provider 桥/指标上报，提交 1f8ea7ce6/
# 35694fc08）。顺带修复：metrics-admin-query 闭包参数遮蔽全局插件实例
# （plugin_runtime lifecycle 计数被降级静默吞掉恒空）；登记
# tests/test_isolation_service_server.py 进插桩基集（此前未收集）。
# 环境配方注：AGENTOS_PAYLOAD_DIAG 保持缺省（=1 会违反
# test_hook_not_installed_by_default 契约且不改覆盖实测）。
# 下一批目标 = task_submit/tool(123 缺行)、context_window_guard(114)、
# isolation/manager(113)、bash/process_manager(93)、security_check(84)。
#
# 收紧（2026-09-13 覆盖率补测批四）：棘轮 92→94——插桩车道全量实测
# 93.19%（32018/34336，8048 测试 0 失败，uv/3.12 口径 598s），floor+1=94
#（棘轮公式）。增量来源 = 本批八件补测：task_submit/tool 缺行 123→0
#（102 用例，白名单登记 tests/test_task_submit_tool_branches.py）、
# context_window_guard 126→3（86 用例，余 3 行为不可达防御分支）、
# isolation/manager 113→17（49 用例）、bash/process_manager 93→2（61
# 用例）、security_check 联合车道 84→4（62 用例）、llm/adapter 106→79
#（36 用例，模块名口径 99% 但官方车道口径仍有余量）。环境配方教训：
# 官方车道 = uv run --frozen（.venv 3.12）+ PYTHONPATH=plugins;sdk/src，
# shell 全局 python（3.14）口径不与车道同分布，本批此前手验全走 3.14
# 系口径偏差（已改用车道口径复验）。
# 下一批目标 = dsh_adapter/server(84)+translator(80)（并行会话进行中，
# 勿撞车）、triggers_ext/manager(58)、monitoring/performance_monitor(57)、
# connectors/server(57)、scene/server(56)、isolation_guard(55)、
# _workspace_git_ops(54)、memory/tool(51)。
# 批五复测（2026-09-13，官方 uv 口径）：实测 93.99%（32272/34336，8279 用例
# 0 失败，564s），floor+1=94 与现基线持平——数值不动。本批增量 = connectors/
# server 57 缺行清零（11 用例）、scene/server 56→2（15 用例）、security_check
# 七文件扩充（+894 行）、dsh_adapter 125 用例（批四目标头两件）、task_submit
# 137/bash 66 用例收尾；另修 agent_prompt_viewer 子进程 120s 超时抖动（→300s）。
# 下一批目标 = triggers_ext/manager(58)、monitoring/performance_monitor(57)、
# isolation_guard(55)、_workspace_git_ops(54)、memory/tool(51)、db_admin(50)。
#
# 收紧（2026-09-13 覆盖率补测批六，接批五目标清单）：94.00→95.00——插桩车道
# 全量实测 94.95%（32642/34367，8458 通过 0 失败 57 跳过，uv/3.12 口径
# 520s），floor+1=95（棘轮公式）。增量来源 = 本批五件补测（207 用例）：
# memory 三件 tool/server/backend 81%→100%（45 用例，backend 未注入守卫族/
# 缺参异常翻译/update 原生 vs 降级分流/server 降级链，提交 d42c3dcee）、
# monitoring/performance_monitor 44 缺行清零 98%（22 用例，监控循环三退出
# 路径/零间隔防除零/同步兼容三环境分支/健康四档，提交 2e79e3ded）、
# isolation_guard 77%→98%（27 用例，早退族/复检异常态/force_host 冲突/
# task_service 三态/server 适配层，提交 3bc6c608b）、
# isolation/_workspace_git_ops 58 缺行清零 100%（24 用例，锁重试族/init
# 双败/prune 修复/符号链接分支，提交 74ea2c7f2）、triggers_ext 93%→98%
#（61 用例，condition_parser 语法错误族/manager 检查循环守卫族/http_api
# 分发面，提交 efb147d9e）。同刀清偿 mypy 3 错（plugins-mypy-baseline 门禁
# HEAD 红：await_args 判空×2 + 动态基类 type() 构造×1）。
# 运行态注：run_gates.py plugins-coverage 仍处 --skip 挂起（执法未恢复），
# 本实测为手动直跑插桩命令（instrumented_args shlex 引用装配，此前两次
# 因 -m 空格拆词/工作区漂移 0 收集——复跑务必用 shlex 引用形态）。
# 下一批目标 = db_admin(50)、worktree_merge(21 缺行)、tasks/http_api(27)、
# hindsight_memory/server(19)；死代码待拍板 = isolation_guard plugin 672/681、
# manager 299-301/858-859/889、condition_parser 357。
#
# 收紧（2026-09-13 覆盖率补测批七）：95→97——插桩车道全量实测 96.60%
#（33198/34367，8848 通过 0 失败 57 跳过，uv/3.12 口径 540s，直跑脚本
# subprocess 列表装配），floor+1=97（棘轮公式）。增量来源 = 本批十四件补测
#（约 400 用例，556 缺行清偿）：llm 四件 adapter 79→1（45 用例，余 1 行
# worker 装箱竞态窗口防御分支）/server 36→0（13）/deepseek 26→0（17）/
# routes_llm_config 23→0（19）、db_admin/server 50→0（42+17）、rollback/
# reversers 47→0（21）、tool_schema_validator 48→0（+40）、fs_tools 44→0
#（30）、browser tool+bridge 72→0（41）、bash 30→0（44）、godot_context/
# server 38→0（+19）、llm_core/plugin 38→0（26）、monitoring/
# execution_records 37→0（16）。同刀披露：批七新测试文件两处裸名缓存/sys.path
# 抢位毒化邻位测试（adapter→llm streaming、models→workspace），已改文件内
# 自清（提交 400f922b6）；中央逐出钩子 trylast 被内建 collect_file 短路属
# 既存缺陷待拍板（对 plugins/** 树无庇护是 workspace 测试加载器盲信
# sys.modules 的合流诱因）。
# 下一批目标 = cost_control/budget_manager(36)、dsh_adapter/bridge(35)、
# multimodal/asr(30)、approval/server(29)、agent_manager/server(29)、
# param_inject(28)、tasks/http_api(27)、monitoring/routes_search(26)、
# context_build(26)、task_evaluate/tool(25)、isolation/workspace(25)。
#
# 收紧（2026-09-13 覆盖率补测批九，深夜）：97→99——插桩车道全量实测 98.11%
#（33865/34518，9336 通过 0 失败 58 跳过，uv/3.12 口径 772s，直跑脚本
# subprocess 列表装配），floor+1=99（棘轮公式）。增量来源 = 本批 12 群补测
#（约 290 用例，430 缺行清偿）：cost_control budget_manager+config（38 用例，
# config 13 缺行清零 100%）、dsh_adapter bridge（19 用例 22 展开，共跑
# 100%）、multimodal asr/adapter/server（41 用例，asr/adapter 100%）、
# approval server（36 用例，29 目标清 28 余 1 结构性防御）、agent_manager
# server（33 用例，共跑 100%）、param_inject（新 29 用例 + 既有 9 用例随
# 目录首次接线）、review media_reviewer+server（34 用例，45 目标缺行全清，
# 顺修 GPSTags→GPSTAGS Pillow12 真缺陷=GPS EXIF 提取静默失效）、
# context_build（36 用例，共跑 100%）、task_evaluate 深分支（22 用例，25 清
# 23 余 2 死防御）、isolation workspace+workspace_lifecycle（36 用例，
# workspace 100%）、download tool（21 用例，共跑 100%）、杂件 _host/
# workspace_service/tasks server（39 用例，workspace_service 100%）。
# 车道治理三连修（均批九全量车道实锤）：①cli_output_adapter 三处
# get_terminal_size 桩 INTERNALERROR——pytest 进度写入器在报告窗口调
# get_terminal_size(fallback=...)，46% 刻度恰落该用例（批三引入，批三~八
# 全量车道因刻度位置未耦合而侥幸）；②test_media_review 假 PIL sys.modules
# 裸赋值外泄毒化后续真实 Pillow 测试；③host 缺口测试裸名 plugin 抢注需
# autouse 前置逐出。并行会话代提 context_build 一提交（8d1770ec2）已对账
# 认可。下一批目标 = duplicate_check(20)、project_registry(19)、
# task_reminder(19)、pipeline/workspace_lifecycle(19)、task_form(18)、
# task/tool(16)、isolation/manager(16)、resource_merge(15)。

# 收紧（2026-09-16 覆盖率收口批）：99→100——插桩车道全量实测 99.8361%
# （36553/36613，11129 通过 9 失败 59 跳过，uv/3.12 口径 1495s）。增量来源 =
# 本批 16 件补测（约 380 用例，320→60 缺行）：eval_harness/server.py
# 88→1（账本读写/聚合基线取数/held-out 三失败分支/提案三工具全链含真实
# git 提交与篡改存档复核）、eval_harness 纯函数面三件 100%（classify 未归类/
# proposal 七条 schema 短路与逃逸/suite_reader workspace 锚定三形态）、
# task_submit/server.py 21→0（on_load 三能力桥 + 工具包装载荷）、rollback
# server+manager 100%、media 五件（_media_core/image/tts/music/video）
# 全 100%、artifacts/models、monitoring/server 靶行清零、isolation_guard、
# context_build server+plugin、bash tool、fs_tools、tasks/reconcile、
# workspace_lifecycle、wsl_native_provider（27 靶行，含 is_available 两环
# 失败/metadata 写失败转 ERROR env/_exec 超时归一）。车道治理一刀：
# tests/test_task_submit_server_wiring.py 未登记 BASE_TEST_PATHS 时不在收集
# 范围（server.py 21 缺行曾因此回涨），已登记（基集 138 条）。
# 已知残留与性质（勿按数字硬刷）——isolation/manager.py 1483-1531 为
# _repair_host_mount 死分支（簇E 穷举已证 PurePath.parts[0] 恒盘符，待
# 拍板）、isolation_guard/plugin.py 779/788、workspace_lifecycle.py 332-342、
# tasks/server.py 91-93 均经 AST 验证结构性不可达；workspace/server.py
# 323-324/613-620 与 task_submit/tool.py 2315-2316 等属需真实宿主上下文的
# 残留。下一批目标 = 上述可达残留 + 新暴露缺行。
# 实测值 99.8361% → 棘轮 floor+1 = 100.00（恒高于实测留压力）。
python_line_coverage=100.00

# 收紧（2026-09-10 补记归因；87.00 引入提交 82c10b5b1，2026-08-27）：棘轮
# 83→87——全绿跑实测 86.02（0 失败，357s），floor+1=87（自动棘轮写入）；
# 增量 = tools/task 99/simple 100/ws_lifecycle 98/level_guard+env_lifecycle
# 100/hindsight 98/渠道 feishu+common 100 六批 + 三条串扰修复上车道；当日
# 累计 74.68→86.02（+11.34pp）。本文件当时未留归因段（仅 commit message
# 持有），2026-09-10 依 git log -S 回溯补记；数值不改。

# 收紧（2026-08-25，批次F 存量绿灯测试接线）：本地实测 69.64%（全车道
# 2870→3461 测试，+591）。来源：plugins/shared 23 个目录/文件的进程内存量
# 测试接入插桩车道（context_window_guard/security_check/isolation/
# hindsight_memory/prompt_build/workspace 等——此前虽有测试但车道不收集，
# 对应模块覆盖率长期失真），另修 tzdata 缺失（Windows zoneinfo 回退 UTC）
# 与 6 处裸名/双实例串扰。基线 70.00 = floor(69.64)+1（自动棘轮写入）；
# CI（Linux）首跑实测后按本地-CI 历史差（约 -0.5pp）以 CI 实测按同公式重定。

# 收紧（2026-08-23，CI 治理批）：本地实测 63.17%（16994/26902 行，全车道
# coverage.xml 产出）。来源：串扰修复使 200+ 既有测试转绿进入插桩执行 +
# 死代码清理缩小分母（game_engine/cua/e2b_provider 等 -1700 行）。
# 基线 64.00 = floor(63.17)+1（自动棘轮公式），即刻红为预期压力，
# 下一批补测达 64 后转绿再按同公式上调。CI（Linux）首跑实测后以 CI 值
# 按同公式重定（本地-CI 历史差 -0.55pp）。

# 收紧（2026-08-21，车道接线+补测批）：本地实测 47.48%（12356/26025，
# run_gates plugins-coverage 门禁内产出）。既有绿灯测试目录接线进插桩车道
# （python_packager/monitoring/download/triggers_ext/workspace_aware，
# scripts/coverage_exempt.py）+ performance_monitor 等 5 个新测试文件贡献。
# 基线 48.00 = 47.48 + 0.52（用户公式：实测+~0.5），即刻红为预期压力，
# 下一批补测达 48 后转绿再按同公式上调。CI（Linux）首跑实测后以 CI 值
# 按同公式重定（本地-CI 历史差 -0.55pp：本地 44.32 vs CI 43.77）。

# 收紧（2026-08-22，死代码清理+四弱模块补测批）：本地实测 56.89%（车道
# 产出 coverage.xml；四模块补测 124 用例：_message_normalizer 3.8%→88%、
# error_classifier 20.3%→95.9%、cost_control/tokenizer 10.7%→96.2%、
# bash/encoding 25.4%→97%，另有 plugins/shared 死代码清理 -320 行）。
# 基线 57.00 = floor(56.89)+1（自动棘轮写入）；CI 首跑后按 -0.55pp 历史差
# 以 CI 实测按同公式重定。
#
# 补测批（2026-09-17，dev/0.2@7490d0077 后实测）：fuse 合并新入六模式包
# （mode_{coding,godot,planning,research,roleplay,writing}/server.py，3297 行）
# 曾把实测砸到 98.38（622 缺行）——本批 58% 清偿后回到 98.96（402 缺行）。
# 增量来源 = 六包降级/容错参数化补测（on_load 双面+warn-once+messages 契约+
# coding 形态面，b4ccda0f0）+ 小残留 30 行（worktree gitdir 失败分支/project_
# registry 守卫/eval_harness 三件/task_evaluate bash 分支/双 sys.path 自举，
# 7490d0077）。**余 402 行分诊**：378 行 = 六包写动作长尾（planning create_
# project/plan、roleplay play/regenerate、research start/followup、writing
# chapter_act、godot dispatch/open_editor 等——需假 tool-executor 逐模式驱动，
# 下一批主菜）；human/server.py 18 行与 tasks/storage.py 4 行 = 并行会话新入
# 代码（非本批回归）；context_build/plugin.py 385 行 sys.path 守卫已有测试
# （dual_root 文件）疑车道未收集，待查 BASE_TEST_PATHS。
