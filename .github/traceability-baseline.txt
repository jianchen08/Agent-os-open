# 测试追溯基线（未标记 @feature 的测试文件数上限，只减不增）
# 见 scripts/check_test_traceability.py（阶段 5.1/5.2）
# 治理一批未标记文件后，运行 --init 收紧该数值。
#
# 收紧（2026-09-08，tests+scripts+config 辖区修复批）：300 → 291（--init 现状值）。
# 门禁在 HEAD 处曾红（unmarked 303 > 帽 300 + FP-D1 非法标记 2 处）：12 个
# tests/tools/builtin/bash 测试按同目录既有惯例补标 FP-0.2.spill_guard、
# 2 个前端 auth 测试 FP-D1（不存在于 test_traceability.md）改映射 FP-0.2.四。
# 注意口径：本帽为 checker 全仓口径（Python test_*.py + 前端 *.test.ts(x) +
# Rust tests/含 src 内嵌，实测 825 文件），非 tests/ 单目录口径。
unmarked_files=291
