# 后端降级按需设计审查报告

> 审查日期: 2026-06-04  
> 审查范围: `src/` 全部 Python 代码  
> 依据原则: 降级/Fallback、兼容性、功能完整性、渐进降级、策略分层、兼容性铁律

---

## 审查结论

**共发现 3 类 23 处违规，其中最严重的 3 项建议优先修复。**

核心问题两条线：
1. Pipeline 插件链内置了 FALLBACK/RETRY 策略体系——内部模块失败不该有"坏了一个用 fallback_state 替代"的设计
2. 多个核心目录存在大面积的双字段名/新旧格式兼容——项目是自己可控的，不该同时维护两套命名

---

## 一、规则1 违规：降级/Fallback 仅限外部依赖 [error]（7处）

### 1.1 【★★★ 最严重】Pipeline 插件链 FALLBACK 策略体系

**涉及文件**: `pipeline/chain.py:77-79, 129-155`, `infrastructure/error_policy.py:49-50`  
**影响范围**: 下列 4 个输入插件全部设置了 `error_policy = ErrorPolicy.FALLBACK`

| 插件 | 文件 | 失败时返回 | 后果 |
|------|------|-----------|------|
| `context_build` | `plugins/input/context_build/plugin.py:45-52` | `system_prompt=""`, `agent_name=""` | Agent 没有 system_prompt 和名字 |
| `tool_schema` | `plugins/input/tool_schema/plugin.py:41-44` | `tool_schemas: []` | Agent 看不到可用工具 |
| `tool_context` | `plugins/input/tool_context/plugin.py:53-57` | 空结果 | Agent 缺少工具约束信息 |
| `knowledge_inject` | `plugins/input/knowledge_inject/plugin.py:43-44` | `knowledge.context: ""` | Agent 没有知识库上下文 |

**违规理由**: 这些是管道的关键输入准备步骤。失败后返回空值让管道继续，等于让 Agent 在"瞎子"状态下运行。应直接 ABORT 而不是静默吞掉。

### 1.2 【★★★ 最严重】Pipeline 插件链 RETRY 策略

**文件**: `pipeline/chain.py:129-149`
```python
if policy == ErrorPolicy.RETRY:
    max_retries = getattr(plugin, "max_retries", 3)
    base_delay = getattr(plugin, "retry_delay", 1.0)
    for attempt in range(1, max_retries + 1):
        delay = base_delay * (2 ** (attempt - 1)) * (0.5 + random.random() * 0.5)
        await asyncio.sleep(delay)
        raw_result = await plugin.execute(ctx)
```

**违规理由**: 插件是纯内部逻辑，执行失败说明有 bug 或输入有问题，指数退避重试没有意义。RETRY 适用于网络请求等外部不可控场景。

### 1.3 【★★★ 最严重】Memory Service 内部检索器间降级链

**文件**: `memory/service.py:447-654`
```python
vector检索 → 失败/不可用
  → keyword检索 (fallback)
    → 不可用
      → _service_layer_fallback (服务层直接查 KnowledgeService)
```
- 配置开关 `_fallback_to_keyword` (第89行) 就是为这种设计存在的
- 异常发生时也降级到关键词检索 (第638-650行)

**违规理由**: `vector`、`keyword`、`KnowledgeService` 都是内部模块，内部模块不可用不应静默降级到另一个。

### 1.4 cost_control 插件 — 计算失败用默认预算兜底

**文件**: `plugins/input/cost_control/plugin.py:98-113`
```python
except Exception as exc:
    fallback_budget = self._default_budget
    return PluginResult(state_updates={
        "cost_control.budget": fallback_budget,
        "cost_control.fallback": True,
    })
```

**违规理由**: 成本控制计算失败说明核心逻辑有问题，用默认预算兜底意味着下游可能绕过成本控制。

### 1.5 stream_bridge — 异常时拼接不完整内容伪装完整响应

**文件**: `pipeline/stream_bridge.py:827-861`
```python
_fallback_content = "".join(self._accumulated_content)
await self._send_event(self._make_event("stream_end", {
    "full_content": _fallback_content, "error": True, ...
}))
```

**违规理由**: 流式中途失败，往前端发不完整内容冒充 `stream_end`，前端无法区分完整/不完整结果。应只发错误事件。

### 1.6 isolation_guard — Docker 不可用时降级到 host 模式

**文件**: `plugins/input/isolation_guard/plugin.py:349-386`
```python
if isolation.value == "container" and not self._docker_available:
    if policy.fallback == "allow":
        return self._build_context(tool_name, "host", "policy_fallback", ...)
```

**违规理由**: `policy.fallback == "allow"` 配置开关编码了"允许不安全降级"的逻辑。Docker 不可用应显式报错。

### 1.7 pipeline/message_bus — 管道复活 force fallback 到默认 Agent

**文件**: `pipeline/message_bus.py:357-378`
```python
# BUG-FIX 注释: agent_config=None 导致 allow_default_fallback=False 抛 ValueError
# "修复": 强制改为 allow_default_fallback=True
```

**违规理由**: 这个"修复"不是解决根因（为什么 agent_config 为 None），而是放宽约束允许 fallback，用一个兜底掩盖设计缺陷。

---

## 二、规则2 违规：兼容性仅限外部依赖 [error]（11处）

### 2.1 【★★★ 最严重】evaluation.py — 双字段名 + 旧格式兼容

**文件**: `core/results/evaluation.py:126-271`

3 个兼容属性：
```python
@property
def message(self) -> str:
    """兼容旧字段名"""  # 实际字段名是 output

@property
def execution_time_ms(self):
    """兼容旧字段名"""  # 实际字段名是 duration_ms

@property
def details(self):
    """兼容旧字段名"""  # 实际字段名是 metadata
```

1 个旧格式解析函数：
```python
@classmethod
def from_legacy_format(cls, data, ...):
    # 0-1 分数 → 0-100 自动转换
    # message/output 双字段名
    # details/metadata 双字段名
    # execution_time_ms/duration_ms 双字段名
```

### 2.2 【★★★ 最严重】core/errors.py — 30+个旧版错误码 + 4个映射表

**文件**: `core/errors.py:108-488`
```python
# 标注 "兼容旧版错误码（从 api/errors.py 迁移）"
AUTH_001 = "AUTH_001"
AUTH_002 = "AUTH_002"
... (30+ 个 AUTH_*/TASK_*/TOOL_*/SYS_*/VAL_*/MEM_*)
```
对应 4 个映射表：消息映射(行217)、严重程度映射(行320)、建议操作映射(行423)、HTTP 状态码映射(行487)。

**违规理由**: 新旧两套错误码并存，映射表维护成本高且容易不同步。既然标注"从 api/errors.py 迁移"，说明迁移完成后应删除旧码。

### 2.3 tool.py — 双字段名兼容

**文件**: `core/results/tool.py:29-61, 72-73`
```python
@property
def data(self) -> Any | None:
    """兼容旧字段名，等同于 output"""

@property
def duration(self) -> float:
    """兼容旧字段名，将 duration_ms 转换为秒"""

# to_dict 同时输出两套字段
if not slim:
    if self.output is not None:
        result["data"] = self.output
```

### 2.4 tasks/storage.py — 旧格式 YAML 持续兼容

**文件**: `tasks/storage.py:64-107`
```python
# 注释: "向后兼容旧的顶层 *.yaml 文件（包含 tasks 列表的旧格式）"
# 加载后不删除原文件，保持向后兼容。
```

**违规理由**: 每次启动都扫描加载旧格式 YAML，旧格式数据应一次性迁移转换。

### 2.5 tool_sync_service.py — ORM 双字段名 (`input_schema`/`args_schema`)

**文件**: `services/tool_sync_service.py:306-307, 339-341, 395-397`
```python
# 写入时双写
input_schema=tool.input_schema, args_schema=tool.input_schema,
output_schema=tool.output_schema, return_schema=tool.output_schema,

# 读取时优先新、兼容旧
input_schema = db_tool.input_schema or db_tool.args_schema or {}
```

### 2.6 window_info.py — `_from_legacy_format` 旧格式解析

**文件**: `bridge/window_info.py:76-134`
```python
# docstring: "旧格式（测试或遗留代码）：title/app/bounds:{x,y,width,height}"
# 格式检测: "processName" in raw vs "app" in raw
```

### 2.7 infrastructure/db.py — 多个"兼容旧 db.connection 接口"

**文件**: `infrastructure/db.py:196-254`
```python
class _DbSessionContext:
    """兼容旧 db.connection 接口"""

async def get_current_session():
    """兼容旧 db.connection 接口。当前实现返回 None。"""
```

**违规理由**: 4 个函数/类全部标注"兼容旧接口"，`get_current_session()` 返回 `None` 是危险的假实现。

### 2.8 LLM Clients — 标注"兼容旧代码"的内部方法

**涉及文件**: `llm/clients/openai.py:748`, `ollama.py:240`, `anthropic.py:237`, `mock.py:195`
```python
def _convert_messages(self, messages):
    """兼容旧代码"""
```

### 2.9 expect_evaluator — 旧字段格式判断

**文件**: `evaluation/expect_evaluator.py:228-265`
```python
# 兼容旧的 expect.success 和 expect.approved 格式
approved = expect.get("approved")
success = expect.get("success")
```

### 2.10 message_logger — "回退到旧格式"

**文件**: `llm/message_logger.py:171-174`
```python
else:
    # 回退到旧格式
    result += f"  - {tool.name}: ..."
```

### 2.11 memory/types.py — ToolInfo 使用旧命名 `args_schema`/`return_schema`

**文件**: `memory/types.py:189-191`
```python
@dataclass
class ToolInfo:
    args_schema: dict = field(default_factory=dict)    # 旧名
    return_schema: str = ""                             # 旧名
```

---

## 三、规则3 违规：过度兜底逻辑（5处）

### 3.1 routes_threads.py — exec_storage 不可用时从 MemoryStore._messages 读

**文件**: `channels/api/routes_threads.py:939-966`
```python
# exec_storage 不可用：尝试从 MemoryStore 的 _messages 读取
# 每个字段用硬编码默认值兜底
id=m.get("id", ""), role=m.get("role", "user"), ...
```

**违规理由**: 两个不同数据源之间不应该有静默 fallback，用 `_messages`（私有字段）作为公共 API 的数据源更是架构问题。

### 3.2 wecom/adapter.py — 未知消息类型 `str(raw)` 兜底

**文件**: `channels/wecom/adapter.py:399-400`
```python
# 其他类型降级
return content or str(raw)
```

**违规理由**: 未知消息类型应报错让开发者处理，而非 `str(raw)` 假装正常。

### 3.3 pipeline/engine.py — `allow_default_fallback` 设计

**文件**: `pipeline/engine.py:129, 201-206`
```python
allow_default_fallback: bool = True,
# CLI/WebSocket 为 True；TaskWorker 为 False
```

**违规理由**: 用布尔标志区分两种使用场景，本质是把类型约束降级为运行时判断。应让所有调用方显式提供 agent_config。

### 3.4 isolation_guard — `task_metadata_fallback` 路径

**文件**: `plugins/input/isolation_guard/plugin.py:349-353`
```python
return self._build_context(
    tool_name, "host", "task_metadata_fallback", workspace=metadata_workspace,
)
```

**违规理由**: metadata 信息缺失时静默降级到 host 模式。

### 3.5 cost_control 插件 — 多个硬编码默认值

**文件**: `plugins/input/cost_control/plugin.py`  
`_default_budget`, `_default_token_limit` 等字段在正常流程中就有硬编码回退。

---

## 四、严重性分级与修复建议

### P0（立即修复）

| # | 问题 | 修复方向 |
|---|------|---------|
| 1.1 | 4个输入插件 FALLBACK 策略 | 改为 ABORT，失败时直接终止管道 |
| 1.2 | Pipeline 插件链 RETRY 策略 | 删除，插件失败直接抛异常 |
| 1.3 | Memory Service 检索器间降级链 | 删除 `_fallback_to_keyword` 开关和降级逻辑 |
| 2.1 | evaluation.py 双字段名+旧格式 | 删除兼容属性、`from_legacy_format`，旧数据脚本迁移 |

### P1（本周修复）

| # | 问题 | 修复方向 |
|---|------|---------|
| 2.2 | errors.py 30+旧错误码 | 完成迁移后删除旧的4个映射表 |
| 2.3 | tool.py 双字段名 | 删除 `data`/`duration` 属性，旧调用方迁移 |
| 2.7 | db.py 4个兼容函数 | 旧调用方统一迁移后删除 |
| 1.4 | cost_control 异常兜底 | 异常直接抛出，不返回默认预算 |
| 1.5 | stream_bridge 拼接不完整内容 | 只发错误事件，不发 stream_end |

### P2（下个迭代）

| # | 问题 | 修复方向 |
|---|------|---------|
| 2.4-2.11 | 其余8项旧格式兼容 | 逐项完成迁移后删除兼容代码 |
| 3.1-3.5 | 过度兜底 | 缺失数据时报错，不静默填默认值 |
| 1.6 | isolation_guard fallback | 删除 `policy.fallback == "allow"` 路径 |
| 1.7 | message_bus force fallback | 确保 agent_config 在调用链上传入 |

---

## 五、核心结论

**根因**: 后端代码存在两个系统性反模式：

1. **Pipeline 插件链的 FALLBACK/RETRY 体系** — 框架级内置了"内部模块失败也不要紧"的哲学，导致多个关键输入插件失败时静默返回空值，Agent 在缺失上下文的环境下运行。

2. **新旧字段名并存** — `evaluation.py`/`tool.py`/`errors.py`/`tool_sync_service.py`/`window_info.py` 等文件同时维护两套命名，注释明确标注"兼容旧字段名""兼容旧格式"。这是内部迁移不彻底的表现。

**修复原则**:
- 内部模块失败 → ABORT/抛异常，不 FALLBACK
- 字段重命名 → 一次性迁移所有引用，删除旧名
- 数据格式变迁 → 脚本批量转换旧数据，删除生产代码中的格式分支
- 外部依赖失败 → 才可以考虑重试/降级（如 LiteLLM 调用、WebSocket 连接等）
