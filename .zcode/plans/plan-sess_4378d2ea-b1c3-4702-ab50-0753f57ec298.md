# 启动脚本与配置环境适配修复 + 测试(最终版)

> 与 v2 差异:工作空间(isolation_config 的 root)**不改任何代码**,只在 README 说明让用户自行配置。

## 改动清单

### A. 端口不一致修复
1. `frontend/.env.example` 行 11/13/17/19 — `8888` → `8988`
2. `tests/test_port_config.py` — 参数化列表加入 `frontend/.env.example`;新增 `TestFrontendEnvExample` 断言 `:8988`

### B. 容器名/网络/卷去硬编码后缀(三处同步)
compose 顶部加 `name: ${COMPOSE_PROJECT_NAME:-agentos}`,去 `-22404` 字面量。
3. `docker-compose.yml` 行 21/40/59/71/73/82/85 + 顶部 `name:` 字段
4. `start_web_cn.bat` 行 278/280/281/285/287/288
5. `wsl_ensure_containers.sh` 行 116/117 改前缀匹配

### C. 裸跑默认值 & .gitignore
6. `wsl_ensure_containers.sh` 行 13 — 裸跑默认路径改 `wslpath` 动态推导
7. `.gitignore` — 加入 `.env`(不删已入库内容)

### D. 文档
8. `README.md` — 补配置说明:工作空间需改时编辑 `config/isolation/isolation_config.yaml` 的 `workspace.root`(让用户自己改);多实例用 `COMPOSE_PROJECT_NAME`;修正方式二端口 `5188`→`5289`

### E. 新增环境适配测试
9. 新建 `tests/test_startup_env_adaptation.py` — 静态断言范式,覆盖:三脚本不含 `22404`;统一容器名;compose 含 `name:`/`COMPOSE_PROJECT_NAME`;wsl 脚本含 `wslpath`;`.gitignore` 含 `.env`

## 不做的事
- ❌ 工作空间 root 不改任何代码(用户自己改配置)
- ❌ 不加环境变量
- ❌ 不删/改已入库 `.env` 内容
- ❌ 不动 install_wsl_docker.sh 2375、searxng 8080

## 验证
- 先跑基线:`pytest tests/test_port_config.py`
- 改后:`pytest tests/test_port_config.py tests/test_startup_env_adaptation.py -v`,期望全绿