[package]
name = "agentos-db-admin"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "DB Admin SQL 能力层 — 通用表 CRUD + SQL 执行器（db-admin capability handler，HTTP 面在 plugins/shared/db_admin 插件）"

[dependencies]
agentos-core.workspace = true
agentos-engine.workspace = true
agentos-http.workspace = true
agentos-mcp.workspace = true
agentos-session.workspace = true
async-trait.workspace = true
axum = { version = "0.8" }
rusqlite = { version = "0.32", features = ["bundled"] }
serde_json.workspace = true
tokio.workspace = true

[dev-dependencies]
tempfile = "3"
