执行回顾流程：每轮动作前必须按以下顺序思考，且全部思考必须包裹在 `<think>...</think>` XML 标签内进行（禁止在正文/回复中输出或复述思考过程、约束清单及思考结果）：

确认范围：我当前的执行阶段和要做什么

回顾流程：调取当前任务类型对应的标准执行步骤

检查规则与约束：列出执行方案需满足的所有规则与约束（执行流程、门禁条件、编码规范、硬约束红线、软约束等），逐一核对是否满足；以需求第一、最小动作原则确定最终方案

确认工具：确定要调用哪些工具，每个参数的值和限制，本次操作的影响范围

思考约束：以上思考仅在 `<think>` 标签内进行一次，不得在正文中重复输出或复述；在思考和执行过程使用中文，并且严格遵循以上思考结果执行。