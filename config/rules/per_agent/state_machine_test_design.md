# 状态机测试设计流程规范

<!--
============================================================
【文件性质】
本文件定义测试生成时识别和覆盖状态机的流程规范。
核心原则：**约束识别状态和分析覆盖的思考流程，而非具体的测试代码模板。**

【使用方】
- test_generator_agent：根据结构化需求中的状态机设计测试
- code_writer_agent：在 TDD Red 阶段参考状态机编写测试
- programming_orchestrator_agent：在门禁 3 中检查状态覆盖是否完整
============================================================
-->

---

## 1. 状态机测试设计流程

### 1.1 总体流程

```
识别状态 → 识别转换 → 设计测试 → 验证覆盖 → 补充遗漏
```

### 1.2 步骤一：识别状态

**目标**：找出组件/模块中所有用户可感知的状态。

**识别方法**：

1. 从结构化需求的 state_machine 中获取已定义的状态列表
2. 如需求未提供状态机，则从代码中识别：
   - 找出所有异步操作 → 每个异步操作至少有 loading/success/error 三态
   - 找出所有条件渲染 → 每个条件分支对应一个状态
   - 找出所有业务规则（如"连续失败 N 次锁定"）→ 锁定是一个独立状态
3. 确保覆盖：初始态、进行中态、成功态、失败态、特殊业务态

### 1.3 步骤二：识别转换

**目标**：找出状态之间的所有转换关系。

**识别方法**：

1. 对每对相邻状态，确认：什么事件触发转换？转换后的可见效果是什么？
2. 检查是否有守卫条件（guard）：某些转换只在特定条件下发生
3. 检查是否有副作用（side effect）：转换时是否触发其他行为

### 1.4 步骤三：设计测试

**目标**：为每个状态和转换设计测试用例。

**设计原则**：

- 每个状态至少一个测试：验证进入该状态后的可见效果
- 每个转换至少一个测试：验证触发事件后状态正确变化
- 每个守卫条件至少两个测试：正向（条件满足，转换成功）和反向（条件不满足，转换被阻止）

**测试必须验证的要素**：

| 要素 | 说明 |
| ---- | ---- |
| 状态值 | 转换后组件/模块的状态是否正确 |
| 可见效果 | 对应的 UI 表现是否正确（如按钮 disabled、文案变化、元素显隐） |
| 副作用 | 转换时是否触发了预期的附加行为 |

### 1.5 步骤四：验证覆盖

**目标**：确认测试覆盖了所有状态和转换。

**覆盖率定义**：

- **state_coverage** = 已测试的状态数 / 已识别的总状态数
- **transition_coverage** = 已测试的转换数 / 已识别的总转换数

**通过标准**：

- state_coverage = 100%
- transition_coverage = 100%
- 所有 must 级别的验收标准都有对应测试

### 1.6 步骤五：补充遗漏

对照步骤一和步骤二的结果，检查是否有遗漏的状态或转换未测试。如有，补充测试用例。

---

## 2. 常见状态机模式（参考）

以下模式用于辅助识别，不是固定模板。具体状态取决于业务需求：

### 2.1 数据获取模式

涉及组件：列表页、详情页、任何从服务端获取数据的场景。

通常涉及的状态：idle → loading → (success / empty / error)

关键检查点：loading 时有无反馈？空数据时有无占位？错误时有无重试？

### 2.2 表单提交模式

涉及组件：登录、注册、设置、任何需要提交数据的场景。

通常涉及的状态：idle → validating → submitting → (success / error)

关键检查点：提交中是否防重复？校验失败是否有定位？提交失败是否可重试？

### 2.3 条件锁定模式

涉及组件：密码重试、限流、任何有次数/时间限制的场景。

通常涉及的状态：idle → counting → locked → (timeout → idle)

关键检查点：计数是否准确？锁定时是否阻止所有操作？解锁后是否恢复正常？

### 2.4 确认操作模式

涉及组件：删除、支付、任何不可逆操作。

通常涉及的状态：idle → confirming → (confirm → executing → success / error / cancel → idle)

关键检查点：确认弹窗内容是否具体？取消是否回到原状态？执行中是否防重复？

---

## 3. 测试质量要求

### 3.1 测试独立性

- 每个测试独立创建组件实例
- 测试之间不共享状态
- 外部依赖必须 Mock
- 测试完成后清理副作用

### 3.2 断言充分性

- 每个测试至少断言**状态值**
- 涉及 UI 的测试还应断言**可见效果**
- 禁止只有"未抛异常"断言的空测试

### 3.3 命名清晰性

测试名称应描述：什么组件 + 什么场景 + 什么预期结果。
具体命名风格遵循项目已有的测试命名规范。

---

## 4. 假性测试防范

### R1 空洞断言禁止 [error]

- 禁止只有 assertTrue(true) 或 assert(true) 的测试
- 禁止断言常量值而非被测逻辑的返回值
- 每个断言必须验证被测代码的实际行为

### R2 边界条件覆盖 [error]

- 必须覆盖正常路径（happy path）
- 必须覆盖边界值（零值、空值、最大值、最小值）
- 必须覆盖异常路径（错误输入、异常场景）
- 参数化测试覆盖多种输入场景

### R3 错误处理验证 [error]

- 必须验证异常处理分支
- 验证抛出正确的异常类型
- 验证异常消息包含有用信息
- 验证异常发生后的状态恢复

### R4 禁止硬编码测试数据 [warning]

- 测试数据禁止硬编码在测试代码中
- 应使用 fixture/factory 模式生成测试数据
- 允许在断言中使用预期的常量值

### R5 副作用验证 [error]

- 必须验证所有副作用（数据库写入、文件操作、外部调用）
- 数据库操作：验证数据确实写入且可读取
- 文件操作：验证文件确实创建且内容正确
- 外部调用：验证调用次数和参数正确

### R6 多维度断言 [warning]

- 对返回值进行多维度验证（类型、结构、关键字段）
- 不仅验证返回值，还要验证状态变化
- 避免单一断言测试：一个测试应验证完整的行为

### R7 测试覆盖标准

- **核心业务覆盖率** [error]: 核心业务逻辑分支覆盖率≥80%
- **公共函数覆盖率** [error]: 所有公共函数覆盖率≥90%
- **整体行覆盖率** [warning]: 整体行覆盖率≥80%
