# 前端降级按需设计审查报告

> 审查日期: 2026-06-04  
> 审查范围: `frontend/src/` 全部代码  
> 依据原则: 降级/Fallback、兼容性、功能完整性、渐进降级、策略分层、兼容性铁律

---

## 审查结论

**共发现 3 类 19 处违规，其中最严重的 4 项建议优先修复。**

核心问题一句话：项目后端 API 是自己控制的服务，但前端写了大量"无论后端返回什么格式我都能跑"的兼容层。这违反了"内部模块间不存在降级/兼容问题——直接统一改"的原则。

---

## 一、规则1 违规：降级/Fallback 仅限外部依赖 [error]（6处）

### 1.1 【★严重】pipelineMessageStore — 内部 API 无条件重试

- **文件**: `stores/pipelineMessageStore.ts:733-750`
- **代码**:
```typescript
const apiResult = await retry(
  () => apiGetMessages(threadId, { ... }),
  { maxAttempts: 3, delayMs: 1000, shouldRetry: ... }
)
```
- **违规理由**: `getMessages` 是内部后端 API，不是外部第三方服务。内部 API 失败应该让调用方看到错误，而不是内置 3 次自动重试。如果后端压力大，重试只会雪上加霜。429/5xx 的 retry 策略应该由 API 层统一管理（如 axios interceptor），不是在每个数据 Store 里各自定义。

### 1.2 【★严重】pipelineMessageStore — threadId 三层 fallback

- **文件**: `stores/pipelineMessageStore.ts:728-732`
- **代码**:
```typescript
const threadId = options?.threadId || sessionFallback || pipelineId
```
- **违规理由**: `pipelineSessionMap` 和 `pipelineId` 都是内部可控数据。threadId 缺失说明调用链有问题，应该报错而不是静默降级为 pipelineId（可能不是正确的 threadId）。

### 1.3 pipelineMessageStore — hasMoreOlder 启发式估算

- **文件**: `stores/pipelineMessageStore.ts:581-582`
- **代码**:
```typescript
[pipelineId]: hasMoreOlder !== undefined ? hasMoreOlder : messages.length >= 50
```
- **违规理由**: 后端不返回 `has_more` 说明 API 契约有问题，前端不应该用 `length >= 50` 猜测。应该让后端始终返回 `has_more`，而不是在前端做启发式兜底。

### 1.4 streaming/index.ts — WS 事件日志的多层字段 fallback

- **文件**: `services/websocket/streaming/index.ts:79-81`
- **代码**:
```typescript
const mid = data.message_id || data.data?.message_id || data.data?.id || ''
```
- **违规理由**: 虽然用于日志，但暴露了 WebSocket 事件字段名不统一的结构问题。

### 1.5 messageHandler.ts — WS 事件的过度字段 fallback

- **文件**: `services/websocket/streaming/handlers/messageHandler.ts:30-32, 44-45`
- **代码**:
```typescript
const messageId = extractMessageId(eventData) || eventData?.message?.id || eventData?.data?.id
content: data?.content ?? existingMsg.content ?? ''
```
- **违规理由**: 内部 WebSocket 事件格式应当统一，不应该在前端为各种可能的字段名做防御。

### 1.6 agentTabStore — pipelineId 反向查找 fallback

- **文件**: `stores/agentTabStore.ts:926-929`
- **违规理由**: `tab.pipelineRunId` 为空说明创建 Tab 时数据不完整，应该在源头上修复，而非在消费侧做反向查找。

---

## 二、规则2 违规：兼容性仅限外部依赖 [error]（7大类，12+文件）

### 2.1 【★★★ 最严重】messages.ts — toMessage() 全字段双名兼容

- **文件**: `services/api/messages.ts:89-239`
- **违规描述**: 函数同时兼容 `snake_case`（数据库直出）和 `camelCase`（前端命名），以及多种同义词。

**具体违规字段对照表**:

| 语义 | 兼容的字段名 | 备注 |
|------|-------------|------|
| 消息ID | `message_id \|\| id` | 双名 |
| 会话ID | `thread_id \|\| sessionId \|\| defaults.sessionId` | 三名 |
| 父消息ID | `parent_message_id \|\| parentId \|\| parent_message_id` | 重复兼容 |
| Agent ID | `agentId \|\| agent_id` | snake/camel |
| 工具调用ID | `call_id \|\| callId \|\| tool_call_id` | 三名 |
| 工具名 | `tool_name \|\| toolName \|\| name` | 三名 |
| 工具参数 | `tool_args \|\| toolArgs \|\| args \|\| parameters` | 四名 |
| 思考内容 | `thinkingContent \|\| thinking_content` | snake/camel |
| 工具结果 | `toolResult \|\| tool_result \|\| output \|\| result` | 四名 |

**影响范围**: 所有通过 API 加载的消息都经过此函数转换。

### 2.2 session.ts — mapBackendMessageToMessage 重复实现相同兼容

- **文件**: `services/api/session.ts:132-262`
- **违规理由**: 与 `messages.ts:toMessage()` 重复实现了几乎相同的 `call_id || callId || tool_call_id` 兼容逻辑。同一个后端 API 的返回值，不应该在两个地方各自防御。

### 2.3 session.ts — 会话 API 显式新旧格式兼容

- **文件**: `services/api/session.ts:398, 456-457`
- **违规代码**:
```typescript
// 注释: "兼容两种格式"、"旧格式"、"新格式"
thread_id: response.data.session_id || response.data.thread_id || ''
if (Array.isArray(response.data)) { ... }
```
- **违规理由**: 代码注释明确标注了"旧格式"兼容。如果迁移已完成，应删除旧格式分支。

### 2.4 【★★ 大面积违规】所有 Store 的 _normalize 函数

- **涉及文件**: `annotationStore.ts:221-233`, `artifactStore.ts:168-182`, `reviewStore.ts:297-328`, `workspaceStore.ts:213-251`
- **共同模式**:
```typescript
id: data.id ?? '',
artifactId: data.artifact_id ?? data.artifactId ?? '',
targetType: data.target_type ?? data.targetType ?? 'whole_artifact',
createdAt: data.created_at ?? data.createdAt ?? '',
authorId: data.author_id ?? data.authorId ?? '',
```
- **违规理由**: 4 个 Store 中重复了相同的 `snake_case ?? camelCase ?? default` 模式。数据源都是内部 API，格式应该已被 API 层统一转换。

### 2.5 agentStore — API 响应字段双名兼容

- **文件**: `stores/agentStore.ts:114, 120`
- **违规代码**:
```typescript
const rawAgents = data.items || data.agents || []  // 兼容两种响应格式
type: agent.agent_type || agent.type || 'atomic'    // 兼容两种字段名
```

### 2.6 toolCardRegistry — 工具参数字段多名称匹配

- **文件**: `utils/toolCardRegistry.tsx:213, 219, 225, 572, 620-623`
- **违规代码**:
```typescript
args.file_path || args.path    // 双名
args.command || args.cmd        // 双名
args.url || args.query          // 双名
goal.title || args.description  // 双名
```

### 2.7 registerWidgets — 明确的"兼容旧代码"

- **文件**: `services/schema/registerWidgets.ts:84`
- **违规代码**: 注释写 `// 兼容旧代码`，将 widget 注册到两个 registry。

---

## 三、规则3 违规：过度兜底逻辑（6处）

### 3.1 所有 _normalize 函数 — 必填字段用 `?? ''` 掩盖错误

- **涉及**: `annotationStore.ts`, `artifactStore.ts`, `reviewStore.ts`, `workspaceStore.ts`
- **违规代码**: `id: data.id ?? ''`
- **违反规则**: 规则3（功能完整性优先）/ 规则5（降级策略分层）
- **违规理由**: `id` 是必填字段。如果后端返回的 `id` 为 null，说明存在严重数据问题。`?? ''` 把数据错误藏了起来。应该让问题暴露（报错或 console.error）。

### 3.2 useInteractionHandler — WS 事件所有字段硬编码兜底

- **文件**: `hooks/useInteractionHandler.ts:84-90`
- **违规代码**:
```typescript
mode: ... || 'choice',        // 关键枚举
title: ... || '',             // 展示字段
threadId: ... || '',          // 路由标识
pipelineId: ... || ''         // 路由标识
```
- **违规理由**: `threadId`/`pipelineId` 是路由关键字段，为空会导致后续路由失败。应该让缺失显性化。

### 3.3 useRealtimeEvents — 执行事件关键字段用占位值

- **文件**: `hooks/useRealtimeEvents.ts:79-81, 170, 191`
- **违规代码**:
```typescript
id: ... || generateUUID(),    // 自动生成 ID
name: ... || 'Unknown',       // 无意义占位
agentName: ... || 'Sub-agent' // 无意义占位
```
- **违规理由**: 自动生成 UUID 会掩盖 ID 丢失问题；占位文本在 UI 上显示无意义。

### 3.4 toolHandler — 工具名 `'unknown'` 兜底

- **文件**: `services/websocket/streaming/handlers/toolHandler.ts:44`
- **违规代码**: `toolName = ... || 'unknown'`
- **违规理由**: `'unknown'` 会直接展示给用户，体验很差。

### 3.5 streamHandler — `?? ''` 掩盖消息数据不完整

- **文件**: `services/websocket/streaming/handlers/streamHandler.ts:216`
- **违规代码**: `content: full_content ?? msg.content ?? ''`
- **违规理由**: 三层 `??` 链，最终 `''` 导致用户看到空白消息。

### 3.6 lifecycleHandlers — notification_id 为空时去重失效

- **文件**: `services/websocket/streaming/lifecycleHandlers.ts:216-219`
- **违规代码**: `notificationId = data?.notification_id || ''`
- **违规理由**: `notification_id` 为空字符串时去重逻辑失效，可能导致重复通知。

---

## 四、严重性分级与修复建议

### P0（立即修复）

| # | 问题 | 修复方向 |
|---|------|---------|
| 2.1 | `messages.ts:toMessage()` 全字段兼容 | 后端 API 统一返回 camelCase，删除前端所有 snake_case 兼容 |
| 1.1 | `pipelineMessageStore` 内部 API retry | 移除 retry，正常错误处理。429 retry 统一在 axios interceptor |

### P1（本周修复）

| # | 问题 | 修复方向 |
|---|------|---------|
| 2.4 | 所有 Store `_normalize` 双名兼容 | 统一数据源格式（API 层），删除 `snake_case` 分支 |
| 1.2 | threadId 三层 fallback | 确保调用链传 threadId，缺失时抛错误 |
| 2.3 | session API 新旧格式兼容 | 删除"旧格式"分支代码 |
| 2.5 | agentStore 响应字段兼容 | 统一后端 API 响应格式 |

### P2（下个迭代）

| # | 问题 | 修复方向 |
|---|------|---------|
| 3.1 | 必填字段 `?? ''` | 改为显式报错或 console.error |
| 3.2-3.6 | WS 事件过度兜底 | 缺失字段时跳过事件处理 + warn 日志 |
| 1.3 | hasMoreOlder 启发式估算 | 后端始终返回 has_more |
| 2.7 | registerWidgets 双重注册 | 完成架构迁移，删除旧 registry |

### 不需要修复的项

| 项目 | 理由 |
|------|------|
| `requestWithRetry` 中的 `enableRetry: false` 默认值 | 默认不做 retry，仅在调用方显式传参时启用，符合设计 |
| `retry.ts` 对 429/5xx 的判断 | HTTP 层的重试策略合理 |
| `GlobalWebSocket` 重连 | WebSocket 连接是对外通道，重连属于正确的降级设计 |

---

## 五、核心结论

**根因**: 项目前后端是同一个 repo，双向可控。但前端代码的写法像是"在对接一个不可控的第三方 API"——大量 `snake_case || camelCase` 兼容、多层 `??` 兜底、内部数据格式判断分支。

**修复原则**: 
1. 后端 API 层统一输出 camelCase，在 `routes` 层做一次转换（如果有需要）
2. 删除前端所有"兼容旧格式"的字段名分支
3. 内部数据缺失时应报错/告警，而不是 `?? ''` 静默掩盖
4. `retry` 仅在对外部服务调用时使用，内部 API 使用普通错误处理

---

## 修复记录（2026-06-04）

### P0（已修复）
| # | 问题 | 修复内容 |
|---|------|---------|
| 1.1 | `pipelineMessageStore` 内部 API retry | 移除 `retry()` 包装，改为直接调用 `apiGetMessages()`，429/5xx 重试由 axios interceptor 统一管理 |
| 2.1 | `messages.ts:toMessage()` 全字段兼容 | 移除所有 snake_case 兼容层，保留 camelCase；标记为 deprecated（当前无模块导入使用，消息转换已迁移到 `session.ts`） |

### P1（已修复）
| # | 问题 | 修复内容 |
|---|------|---------|
| 1.2 | threadId 三层 fallback | 移除 `|| pipelineId` 降级；threadId 不可用时抛 Error 而非静默使用 pipelineId |
| 2.2 | `session.ts:mapBackendMessageToMessage` 双名兼容 | toolCalls 映射移除 `snake_case` 兼容（`call_id`, `tool_call_id`, `tool_args`, `parameters` 等），仅保留 `camelCase` |
| 2.3 | session API 新旧格式兼容 | 删除 `ThreadCreateResponse` 中 `session_id` 字段（后端始终返回 `thread_id`）；删除 `getMessages` 中 `Array.isArray` 旧格式分支 |
| 2.4 | 所有 Store `_normalize` 双名兼容 | `annotationStore/artifactStore/reviewStore/workspaceStore` 四个文件的 `_normalize` 函数移除全部 `snake_case` 兼容（如 `artifact_id ?? artifactId` → `artifactId`）；必填字段 `id` 缺失时增加 `console.error` 告警 |
| 2.5 | agentStore 响应字段兼容 | 移除 `data.agents` 备选格式（后端统一用 `items`）；移除 `agent.type` 备选（后端统一用 `agent_type`） |

### P2（已修复）
| # | 问题 | 修复内容 |
|---|------|---------|
| 1.3 | hasMoreOlder 启发式估算 | 移除 `messages.length >= 50` 启发式（后端始终返回 `has_more`），直接使用 API 返回值 |
| 2.6 | toolCardRegistry 双名匹配 | 保留多工具参数名兼容（不同工具使用不同参数名），添加注释说明是工具级别兼容而非 API 格式兼容 |
| 2.7 | registerWidgets 双重注册 | 更新注释说明为多渲染引擎架构（Composer + WidgetRegistry），移除"兼容旧代码"标注 |
| 3.1 | 必填字段 `?? ''` 掩盖错误 | 四个 Store 的 `_normalize` 函数中 `id` 缺失时增加 `console.error` 告警（保留 `?? ''` 作为类型安全默认值） |
| 3.2 | useInteractionHandler 过度兜底 | `threadId`/`pipelineId` 缺失时增加 `console.warn` 告警 |
| 3.3 | useRealtimeEvents 占位值 | `execution_id` 缺失时增加 `console.warn`；Sub-agent `agentId` 缺失时增加 `console.warn` |
| 3.4 | toolHandler `'unknown'` 兜底 | `tool_name` 缺失时改为 `console.warn` + 跳过事件处理，而非显示 `'unknown'` |
| 3.5 | streamHandler `?? ''` 掩盖 | `full_content` 为 null/undefined 时不再覆盖已有 `content`；两者均为空时增加 `console.warn` |
| 3.6 | lifecycleHandlers 去重失效 | `notification_id` 为空时改用 content 精确匹配去重，防止空字符串匹配所有通知 |

### 保留不修复（补充说明）
| 项目 | 理由 |
|------|------|
| WS 事件字段名 fallback（1.4, 1.5） | WebSocket 事件因事件类型而异，合理的防御性编码 |
| toolCardRegistry 多工具参数名 | 不同工具定义使用不同参数名（file_path/path, command/cmd, url/query），非 API 格式兼容 |
| agentTabStore pipelineId 反向查找 | `pipelineRunId` 缺失时从 `pipelineTabMap` 反向查找是必要兜底，增加 `console.warn` 和缺失时 `return` 避免静默失败 |
