{
  "id": "pipeline_context_build",
  "name": "Context Build",
  "version": "1.0.0",
  "plugin_type": "pipeline",
  "language": "python",
  "host_type": "sidecar",
  "entry": "python server.py",
  "invoke_entry": "context_build.execute",
  "requires_services": ["mode.get_profile"],
  "capabilities": {
    "tools": [],
    "route_signals": [],
    "lifecycle_hooks": [
      "on_load",
      "on_unload"
    ],
    "steps": [
      {
        "name": "pipeline_context_build",
        "required_state_inputs": [
          "agent.id"
        ]
      }
    ]
  },
  "permissions": {},
  "priority": 50,
  "export_fields": [
    "task.id",
    "tool_ids",
    "context.*",
    "model_tier",
    "max_iterations",
    "max_reminders",
    "timeout_seconds",
    "agent_level"
  ],
  "persistent_fields": [
    "task.id",
    "model_tier",
    "max_iterations",
    "max_reminders",
    "timeout_seconds",
    "agent_level"
  ],
  "description": "\n\n[state 出口口径] 本插件真实产出键 = task.id / tool_ids / context.*（含 extra_context 与 agent 配置运行时键）；lineage.* 仅读（出生写面在 task_birth 出生 state，经 chat_send_handler/task_submit 一次写全），display_name/name/tags 取自 agent 配置非本插件写入。",
  "state": {
    "reads": [
      "agent.id",
      "core_type",
      "execution_context",
      "iteration",
      "lineage.parent_pipeline_id",
      "pipeline_id",
      "session_id",
      "system_prompt",
      "task.id"
    ]
  }
}
