[project]
name = "agentos-plugin-mode-coding"
version = "0.1.0"
description = "Lingxi AgentOS 0.2 plugin — mode_coding（编码模式出厂种子）"
requires-python = ">=3.11"
dependencies = [
    "agentos-plugin-sdk>=0.2.0",
    "pyyaml",
]

[tool.uv.sources]
agentos-plugin-sdk = { path = "../../../sdk", editable = true }
