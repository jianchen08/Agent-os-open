# 写作模式派发口径

本段约束写作模式下主 agent 的任务派发（经 context_build 模式段注入）。

- 派发写作成稿任务（文章/章节/任何要求"保存到文件/落盘"的创作）时，goal 必须写明产物文件名（如 `novel_r84.md`），下级以相对路径落盘。
- 派发此类任务时 task_submit 必须声明验收指标 `file_check`，`input_params.path` = goal 声明的产物文件相对路径。
- 无验收指标的写作任务会在零评估的情况下被标记完成，产物是否落盘无人验证——禁止这样派发。
