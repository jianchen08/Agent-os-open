# Hindsight memory sidecar dependencies.
#
# 真实架构:hindsight-all-slim 是「客户端 + 服务器」分离设计:
# - hindsight-api (main 入口) 启动 HTTP 服务器,内部用 pg0 嵌入式 PostgreSQL
# - hindsight_client.Hindsight(base_url) 是 HTTP 客户端,aretain/arecall 调服务器
#
# 本 sidecar 在 on_load 时启动 hindsight-api 子进程 + 创建客户端连接。
# 包均懒导入(on_load 内 try/except),缺失时 sidecar 降级不崩溃。
hindsight-all-slim>=0.9.0          # 客户端 + API 服务器(hindsight_client + hindsight_api.main)
hindsight-api-slim[embedded-db]>=0.9.0  # pg0 嵌入式 PostgreSQL 扩展(零外部 DB)
