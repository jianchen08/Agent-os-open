[project]
name = "agentos-plugin-director-service"
version = "0.1.0"
description = "Lingxi AgentOS 0.2 plugin — director（AI 弹幕共创直播导演面）"
requires-python = ">=3.11"
dependencies = [
    "agentos-plugin-sdk>=0.2.0",
    "pyyaml",
]

[tool.uv.sources]
agentos-plugin-sdk = { path = "../../../sdk", editable = true }
