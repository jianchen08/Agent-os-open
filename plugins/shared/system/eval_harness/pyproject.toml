[project]
name = "agentos-plugin-eval-harness-service"
version = "0.1.0"
description = "Lingxi AgentOS 0.2 plugin — eval_harness_service（自进化评测面）"
requires-python = ">=3.11"
dependencies = [
    "agentos-plugin-sdk>=0.2.0",
    "pyyaml",
    "websockets>=12.0",
]

[tool.uv.sources]
agentos-plugin-sdk = { path = "../../../sdk", editable = true }
