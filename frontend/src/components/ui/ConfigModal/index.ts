/**
 * ConfigModal 组件导出
 *
 * 统一配置模态框组件，支持多种字段类型、验证和暂存功能
 */

export { ConfigModal, useConfigForm } from './ConfigModal'
export { clearDraft } from './useConfigForm'
export type { ConfigFieldType, ConfigField, ConfigModalProps, FormErrors, FormState } from './types'
